schema_version: 1

readme_coverage:
  modules: []

lifecycle_transition_contract:
  allowed_transitions:
    - added
    - moved
    - split
    - merged
    - deprecated
    - archived
  required_roadmap_fields:
    - roadmap.id
    - roadmap.url
    - roadmap.status
