# Desktop UI Workflow Acceptance Matrix

**Last Updated:** 2026-05-27
**Owner lane:** Workstation Shell and UX
**Scope:** `src/Meridian.Wpf`, `src/Meridian.Ui.Services`, `src/Meridian.Ui.Shared`, with `src/Meridian` host and shared workstation endpoints as integration edges
**Status:** Active acceptance matrix for desktop work inside Waves 2-4

## Purpose

This matrix turns the active desktop and shared UI workflow goal into release criteria. WPF work is accepted only when it closes an operator scenario in the Wave 2, Wave 3, or Wave 4 path and consumes shared services, read models, or workstation endpoints before composing the desktop UI.

The target lanes are:

| Lane | Wave | Acceptance focus |
| --- | --- | --- |
| Lane A | W2 | Trading cockpit reliability: paper-session persistence, replay recovery, promotion readiness, and actionable operator work items. |
| Lane B | W3 | Run -> portfolio -> ledger continuity: shared run context, portfolio/account posture, ledger/cash-flow evidence, and reconciliation handoff continuity. |
| Lane C | W4 | Reconciliation/governance close flow: durable casework, sign-off visibility, close/report workflow posture, and audit/result traceability. |

## Acceptance Rules

1. Desktop business behavior must start in shared contracts, shared services, shared read models, or shared workstation endpoints. WPF may compose and present that behavior, but it must not become the only source of wave-critical state.
2. Every WPF change must name the lane, scenario, shared API/read-model check, and focused WPF test it advances.
3. A desktop-browser mismatch against a shared endpoint, DTO, route target, readiness tone, blocker reason, or work-item action is a release blocker for the workflow claim.
4. Support evidence is not an exit claim. A page, hero, route, or queue affordance counts only when the matching happy path, blocker path, and recovery path have evidence.
5. Fixture/demo payloads can support local development, but they cannot satisfy wave acceptance unless the matrix row explicitly marks them as deterministic harness evidence.

## Scenario Matrix

| Lane | Scenario | Operator outcome | Shared-first contract check | Desktop evidence | Browser/parity check | Release blocker if missing |
| --- | --- | --- | --- | --- | --- | --- |
| A: W2 cockpit | Happy path: restore an active paper session, verify replay, inspect cockpit readiness, and route to the next review action. | Operator can confirm the paper workflow is restart-safe and ready for paper operation from shared evidence. | `GET /api/workstation/trading/readiness`, `GET /api/workstation/operator/inbox`, execution replay endpoints, and `PilotAcceptanceHarnessTests` `PaperPromotion` / `PaperSession` gates. | `TradingWorkspaceShellViewModelTests`, `TradingWorkspaceShellPageTests`, `MainPageUiWorkflowTests`, plus focused shared tests such as `Wave2PaperTradingCockpitAcceptanceTests` and `TradingOperatorReadinessServiceTests`. | Browser Trading cockpit and `/trading/readiness` must show the same readiness posture, UTC evidence timing, and next action. | WPF shows ready while replay, DK1 trust, brokerage sync, promotion, or operator-inbox evidence is stale or blocked. |
| A: W2 cockpit | Blocker path: stale replay, execution-control block, promotion-review gap, brokerage-sync blocker, or security/reconciliation blocker is present. | Operator sees a concrete reason, owner/source when available, and a route to the workbench that can clear the blocker. | Shared `TradingOperatorReadinessDto` work items and `/api/workstation/operator/inbox` route metadata, including account-scoped `fundAccountId` when applicable. | `MainPageViewModel` / queue behavior through `MainPageUiWorkflowTests`, `WorkspaceShellContextServiceTests`, `TradingWorkspaceShellViewModelTests`, and `Wave2OperatorInboxAcceptanceTests`. | Browser readiness summary and Operator Readiness Console must use the same work-item route/action semantics. | Direct action exists only in WPF copy, toast text, or page-local logic instead of a shared work item. |
| A: W2 cockpit | Recovery path: after replay verification or blocker remediation, readiness and queue state refresh without preserving stale blocked state. | Operator can rerun the verification/remediation and see readiness downgrade or recover consistently. | Replay audit metadata, readiness recomputation, and inbox refresh from the shared endpoint path. | `TradingWorkspaceShellViewModelTests`, `TradingWorkspaceShellPageTests`, and `Wave2PaperTradingCockpitAcceptanceTests` recovery cases. | Browser Trading refresh must recover to the same effective level and action list. | A stale in-memory WPF state disagrees with the latest shared readiness or inbox payload. |
| B: W3 continuity | Happy path: open a retained run/session and follow it into portfolio, ledger, cash-flow, and reconciliation context. | Operator can explain how the run changed portfolio and accounting state without switching mental models. | `StrategyRunReadService`, `StrategyRunContinuityService`, `PortfolioReadService`, `LedgerReadService`, `CashFlowProjectionService`, reconciliation projections, and `PilotAcceptanceHarnessTests` `PortfolioLedgerReview` gate. | `StrategyRunPortfolioViewModelTests`, `AccountPortfolioViewModelTests`, `AggregatePortfolioViewModelTests`, `CashFlowViewModelTests`, `FundLedgerViewModelTests`. | Browser Strategy, Portfolio, Accounting, and Reporting routes must agree on selected run/session identity and blocker state. | Desktop derives run/portfolio/ledger meaning from page-local state that cannot be reproduced through shared services. |
| B: W3 continuity | Blocker path: missing run context, brokerage/account sync stale state, ledger-count mismatch, missing cash-flow rows, or reconciliation gap. | Operator sees the missing evidence, impact, and next route instead of a blank grid or silent partial view. | Shared continuity warnings, account-sync readiness DTOs, reconciliation read models, and compatibility tests such as `LedgerReconciliationContractCompatibilityTests`. | `FundAccountsViewModelTests`, `CashFlowViewModelTests`, `FundLedgerViewModelTests`, `AccountPortfolioViewModelTests`, `WorkspaceShellContextServiceTests`. | Browser Portfolio/Accounting should expose the same gap and not silently mark the workflow ready. | WPF hides a continuity gap behind an empty state that looks successful. |
| B: W3 continuity | Recovery path: account sync, run context restore, ledger reconciliation, or filter reset makes retained evidence visible again. | Operator can recover without reloading unrelated workflow state or losing selected context. | Shared account endpoints, `BrokeragePortfolioSyncService`, workstation endpoint DTO compatibility, and `PilotAcceptanceHarnessTests` `Reconciliation` gate. | `BrokeragePortfolioSyncServiceTests`, `FundAccountsViewModelTests`, `FundLedgerViewModelTests`, `StrategyRunContinuityServiceTests`. | Browser and WPF must route brokerage-sync work to the same account-scoped destination and use the same readiness reason. | Recovery exists in one client only or depends on a desktop-only business rule. |
| C: W4 governance | Happy path: review reconciliation casework, sign-off posture, close-lane readiness, and report-pack evidence from one governed context. | Operator can trace issue -> decision -> audit/result view for close/report work. | Operations-continuity endpoints, reconciliation case services, report-pack validation, evidence packet/graph links, and `PilotAcceptanceHarnessTests` `GovernedReportPack` gate. | `FundLedgerViewModelTests`, `FundReconciliationWorkbenchServiceTests`, `FundAccountsViewModelTests`, `OperationsContinuityDtoContractTests`, `GovernanceWorkspaceShellPageTests`. | Browser Accounting/Reporting/Evidence Workbench must expose the same case status, sign-off state, and report-pack blocker semantics. | Desktop presents close/report readiness without durable case status, approval/sign-off, or evidence links. |
| C: W4 governance | Blocker path: unresolved reconciliation case, missing tolerance/sign-off metadata, approval gap, report-pack validation failure, or broken evidence chain. | Operator sees severity, owner/role, required sign-off, decision state, and next action. | `ReconciliationCaseService`, `OperationsContinuityWorkflowService`, `ReportPackValidationService`, and reconciliation calibration/readiness endpoints. | `FundLedgerViewModelTests`, `FundReconciliationWorkbenchServiceTests`, `OperationsContinuityDtoContractTests`, `WorkspaceShellContextServiceTests`. | Browser case queue and report-pack task panels must agree with WPF on the blocker reason and route target. | WPF emits governance-ready language from preview data or endpoint-local fallbacks. |
| C: W4 governance | Recovery path: case decision, approval/sign-off, or report-pack regeneration updates queue, close posture, and audit trail. | Operator can resume close/report work and audit who changed what, when, and why. | Durable operations-continuity timeline, reconciliation case audit history, report-pack lifecycle metadata, and evidence graph references. | `OperationsContinuityWorkflowServiceTests`, `OperationsContinuityPostgresRoundTripTests`, `ReconciliationCaseServiceTests`, plus WPF view-model tests for the consuming surface. | Browser and WPF should agree after refresh on case state, approval posture, and report-pack readiness. | Desktop cannot reconstruct the recovery path after restart or refresh. |

## Milestone Evidence

| Milestone | Required green evidence | Required docs update |
| --- | --- | --- |
| Milestone 1: W2 desktop cockpit acceptance | `Wave2PaperTradingCockpitAcceptanceTests`, `Wave2OperatorInboxAcceptanceTests`, `TradingOperatorReadinessServiceTests`, the focused WPF Trading/MainPage tests touched by the slice, and `PilotAcceptanceHarnessTests` with W2 blockers reflected in `TrustedData`, `PaperPromotion`, or `PaperSession`. | Update the relevant W2 row in `wave-implementation-checklists.md`, then link any produced evidence packet from `ROADMAP.md` or a status evidence doc. |
| Milestone 2: W3 continuity workflows | Focused shared continuity tests, focused WPF Portfolio/Accounting/CashFlow/FundAccounts/FundLedger tests touched by the slice, and `PilotAcceptanceHarnessTests` with W3 blockers reflected in `ResearchRun`, `RunComparison`, `PortfolioLedgerReview`, or `Reconciliation`. | Update W3 checklist blockers and any affected shared-contract or source README notes when DTO/read-model behavior changes. |
| Milestone 3: W4 governance/close baseline | Focused reconciliation/casework/report-pack/operations-continuity tests, focused WPF consuming-surface tests, and `PilotAcceptanceHarnessTests` with W4 blockers reflected in `Reconciliation` or `GovernedReportPack`. | Update W4 checklist blockers and the nearest governance/status evidence doc before claiming close/report readiness progress. |

## Current Support Evidence

- 2026-05-27: Lane A and Lane B acceptance evidence is green for W2/W3 closure. The shared
  build-enabled W2/W3 filter passed (`Wave2PaperTradingCockpitAcceptanceTests`,
  `Wave2OperatorInboxAcceptanceTests`, `TradingOperatorReadinessServiceTests`,
  `BrokeragePortfolioSyncServiceTests`, and `PilotAcceptanceHarnessTests`; 71 passed). Browser
  parity passed from `C:\Meridian-local-drive\Meridian-main`
  (`trading-screen.view-model.test.ts`, `trading-screen.test.tsx`, `api.trading.test.ts`,
  `workstation-endpoints.test.ts`, `workspace.test.ts`, and `command-palette.view-model.test.ts`;
  154 passed, 1 existing todo). Focused WPF Lane A/B shell validation passed
  (`TradingWorkspaceShellViewModelTests`, `Wave2OperatorInboxAcceptanceTests`,
  `MainShellViewModelTests`, `StrategyRunPortfolioViewModelTests`, `AccountPortfolioViewModelTests`,
  `AggregatePortfolioViewModelTests`, `CashFlowViewModelTests`, `FundLedgerViewModelTests`, and
  `FundAccountsViewModelTests`; 113 passed). The latest
  `artifacts/pilot-acceptance/latest/pilot-readiness.json` has all W2/W3 mapped gates `Ready` with
  no blockers, so Milestone 1 and Milestone 2 are accepted in the current evidence slice.

- 2026-05-27: W2/W3/W4 route-parity support evidence now covers shared workflow target tags used by the browser command palette, workstation links, and WPF source routing. Browser route tests pin Trading readiness targets (`TradingReadiness`, `TradingReadinessConsole`, `RunRisk`) to `/trading/readiness`, close-lane targets (`OperationsContinuity`, `OperationsClose`) to `/accounting/operations-continuity`, and accounting workflow targets (`FundReconciliation`, `FundTrialBalance`, `FundAuditTrail`) to the same Accounting routes guarded by WPF navigation. Command-palette tests now use the canonical shared accounting workflow action ids for reconciliation breaks, ledger continuity, and audit-trail review. The source-only WPF UX checker also verifies shared paper-trading workflow actions and inbox API routes resolve to WPF `TradingShell` / `RunRisk`, and shared report-pack approval actions/work items resolve to WPF `FundReportPack` and browser `/reporting/report-packs`. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/components/meridian/command-palette.view-model.test.ts src/lib/workspace.test.ts` (2 files passed, 27 tests passed) and `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (10 passed, 0 failed). This is route-parity support evidence; it is not a full W2 cockpit, W3 continuity, or W4 close-flow acceptance claim.

- 2026-05-27: Lane B/C desktop-browser drift guard now checks shared accounting workflow routing across the shared workflow provider, WPF navigation, and browser route catalog. The source-only WPF UX checker verifies that `AccountingReviewReconciliation`, `AccountingReviewLedgerContinuity`, and `AccountingReviewAuditTrail` continue to route to WPF `FundOperations` reconciliation/trial-balance/audit tabs and browser Accounting reconciliation/ledger/audit routes instead of diverging through page-local route logic. Focused validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (8 passed, 0 failed). This closes a W3/W4 drift-risk guard only; milestone acceptance still requires the focused WPF view-model/shared/pilot gates in the matrix.

- 2026-05-27: Lane A recovery-path browser support evidence now exercises the paper-session hook path instead of only the pure projection helper. `usePaperSessionsViewModel` restores an active session, clears stale replay verification, verifies replay through shared detail/replay services, projects the `Matched current state` audit row, and calls the readiness/evidence refresh callback. Lane C close-flow browser support evidence now counts durable break-case evidence in Operations Continuity and keeps the latest `reconciliation-case-resolved` audit context visible with actor, state transition, rationale, audit id, and hash. Focused validation from `C:\Meridian-local-drive\Meridian-main`: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/trading-screen.view-model.test.ts src/screens/operations-continuity-screen.view-model.test.ts` (2 files passed, 74 tests passed, 1 existing todo), `node .\node_modules\typescript\bin\tsc --noEmit --pretty false -p .\tsconfig.json` from `src/Meridian.Ui/dashboard` (passed), and `git diff --check HEAD~1..HEAD` (passed). This remains browser/shared support evidence until the corresponding WPF and shared/pilot gates are green.

- 2026-05-27: Cross-lane WPF shell-hardening support evidence now keeps the Data shell on the shared workspace chrome path by rendering `WorkspaceShellContextStripControl` from the existing `ShellContext` view-model state, and the source-only WPF checker now verifies shared context-strip coverage across the retained top-level WPF shells: Trading, Portfolio, Accounting, Reporting, Strategy, Data, and Settings. Low-disk validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf` (7 passed, 0 failed), plus XML parse coverage for the top-level WPF shell XAML files. This is structural shell evidence only; W2/W3/W4 milestone acceptance still requires the lane-specific WPF test and shared/pilot gates.

- 2026-05-27: Lane A blocker-path and recovery-path support evidence extended to cover operator inbox routing parity and recovery across WPF and browser. New tests added: `Wave2OperatorInboxAcceptanceTests` (7 WPF tests covering tone/label rendering, shared TargetPageTag navigation, fundAccountId threading, mixed-severity prioritization, and inbox-clear recovery), `TradingWorkspaceShellViewModelTests` (2 recovery-path tests: blocker-clears-to-ready and replay-blocker-clears-to-paper-oversight), `MainPageUiWorkflowTests` (`MainPage_OperatorInboxWorkflow_BlockerItemNavigatesToSharedTargetPage`: a Critical PaperReplay blocker routes to `TradingShell` via the shared TargetPageTag — not a desktop-only fallback), and browser `"trading readiness blocker recovery"` describe block (3 tests: `buildTradingReadinessState` drops blocked work items on cleared payload, status announcement transitions from blocked to ready, `useTradingReadinessViewModel` hook applies cleared payload and drops stale work items after refresh). Validation runs pending on the WPF side (Windows/MSBuild required); browser tests are runnable via `npm --prefix src/Meridian.Ui/dashboard run test -- src/screens/trading-screen.view-model.test.ts`.

- 2026-05-27: Lane A blocker-path support evidence now includes shared-route parity for trading readiness work items. WPF `MainPageViewModel` has a focused regression test that a `PaperReplay` work item carrying `TargetRoute = /api/workstation/trading/readiness` and `TargetPageTag = TradingShell` opens the Trading shell instead of falling back to a desktop-only audit page. Browser Trading readiness now respects shared `targetRoute` / `targetPageTag` metadata for execution-control and promotion-review actions while preserving the replay-specific `/trading#session-replay-panel` operator action. Focused validation: `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~OperatorInboxPresentation_UsesSharedTradingReadinessRouteForPaperReplayWorkItems" --no-build --no-restore /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /p:MeridianBuildIsolationKey=w2-inbox-route-20260527 -maxcpucount:1 --logger "console;verbosity=normal"` (1 passed) and `node node_modules/vitest/vitest.mjs run --pool=threads --maxWorkers=1 --minWorkers=1 --reporter=verbose src/screens/trading-screen.view-model.test.ts` from `C:\Meridian-local-drive\Meridian-main\src\Meridian.Ui\dashboard` (63 passed, 1 todo). The broader shared `Meridian.Tests` W2 readiness filter still needs a clean pass; the latest attempt failed during compile with local `ENOSPC`, before test assertions ran.
- 2026-05-27: Lane A browser validation support evidence now has the Trading readiness recovery tests TypeScript-clean after removing unsupported Vitest matcher message arguments from the blocker-clear assertions. Focused validation from `C:\Meridian-local-drive\Meridian-main`: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/trading-screen.view-model.test.ts` (66 passed, 1 todo), `node .\node_modules\typescript\bin\tsc --noEmit --pretty false -p .\tsconfig.json` from `src/Meridian.Ui/dashboard` (passed), and `npm --prefix src/Meridian.Ui/dashboard run build` (passed when rerun outside the sandbox after an initial esbuild `spawn EPERM`). No WPF `--no-build` validation was available because standard WPF test binaries were absent.
- 2026-05-27: Lane B blocker-path support evidence now includes a focused `CashFlowViewModelTests` case proving that a retained run with loaded cash-flow rows still surfaces shared continuity blocker state (`Continuity blocked`, `open-reconciliation-breaks`) instead of presenting the cash-flow ladder as implicitly ready. Focused validation: `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~Parameter_WhenCashFlowsLoadButContinuityIsBlocked_ShouldSurfaceSharedBlocker" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:MeridianBuildIsolationKey=w3-cashflow-blocker-full-20260527 /p:UseSharedCompilation=false -maxcpucount:1 /nr:false --logger "console;verbosity=normal"` (1 passed). This extends the earlier Run Cash Flow continuity posture support, but it is still W3 support evidence only until the broader run -> portfolio -> ledger -> reconciliation path and browser parity checks are green.
- 2026-05-27: Lane C blocker-path support evidence now includes selected Fund Ledger reconciliation-break lifecycle and sign-off posture projected from shared break queue rows into `FundLedgerViewModel`/`FundLedgerPage`. Focused validation: `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~FundLedgerViewModelTests|FullyQualifiedName~FundReconciliationWorkbenchServiceTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false -maxcpucount:1 --logger "console;verbosity=normal"` (9 passed). This is W4 support evidence only; durable close/casework acceptance still requires the operations-continuity and browser-parity gates in the matrix.
- 2026-05-27: Lane B blocker-path support evidence now includes Run Cash Flow continuity posture projected from `StrategyRunContinuityService` into `CashFlowViewModel`/`RunCashFlowPage`. Focused validation: `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~CashFlowViewModelTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false -maxcpucount:1 --logger "console;verbosity=normal"` (15 passed). This is W3 support evidence only; Milestone 2 still requires the broader run -> portfolio -> ledger -> reconciliation happy/blocker/recovery proof and browser parity.
- 2026-05-27: Lane B browser-parity support evidence now keeps the shared `FundTrialBalance` workflow target aligned with the WPF trial-balance route by resolving it to browser `/accounting/ledger` instead of the accounting root. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test -- src/lib/workspace.test.ts src/components/meridian/workspace-nav.view-model.test.ts src/screens/governance-screen.view-model.test.ts src/screens/governance-screen.test.tsx` and `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --filter "FullyQualifiedName~WorkflowLibraryEndpointTests" --no-restore /p:UseSharedCompilation=false -maxcpucount:1 --logger "console;verbosity=normal"`. This is a drift-risk closure for workflow routing; it is not a full W3 continuity acceptance claim.
- 2026-05-27: Lane B browser-parity support evidence now keeps selected-run continuity blocker copy visible in the Portfolio task panel and selected-run detail instead of letting continuity blocker counts fall through as ready/clean text. Focused validation from `C:\Meridian-local-drive\Meridian-main`: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/portfolio-screen.view-model.test.ts src/lib/api.trading.test.ts src/screens/operations-continuity-screen.view-model.test.ts` (51 passed across 3 files). This is W3 support evidence only; Milestone 2 still requires the WPF continuity path and shared/pilot gates.
- 2026-05-27: Lane C recovery-path support evidence now has a focused WPF test for resolving a selected Fund Ledger reconciliation break through the shared workstation queue path. The view model refreshes the queue, keeps the closed break selected under `All`, retains the decision note in the audit rows, and marks close approval blocked while required sign-off remains pending. Focused validation attempted: `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~FundLedgerViewModelTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false -maxcpucount:1 --logger "console;verbosity=normal"`; local WPF/MSBuild startup timed out before returning test output, so this remains support evidence pending a clean WPF validation run.
- 2026-05-27: Lane C browser-parity support evidence now projects shared reconciliation break close metadata in browser Accounting detail: exception route, tolerance profile/band, required sign-off role/status, and decision note remain visible after a break is resolved. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test -- src/screens/governance-screen.view-model.test.ts src/screens/governance-screen.test.tsx`. This closes a desktop-browser drift risk for the Fund Ledger decision-recovery posture; it is not full W4 close/casework acceptance.
- 2026-05-27: Lane C browser-parity support evidence now projects close evidence link coverage in the Operations Continuity selected-workflow detail, counting workflow, approval, report-pack, and break-case evidence links already exposed by the shared DTO. Focused validation from `C:\Meridian-local-drive\Meridian-main`: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/portfolio-screen.view-model.test.ts src/lib/api.trading.test.ts src/screens/operations-continuity-screen.view-model.test.ts` (51 passed across 3 files). This is W4 close-flow support evidence only; durable casework and close/report acceptance still require WPF consuming-surface and shared/pilot gates.

- 2026-05-28: Lane C WPF support evidence now has shared desktop affordance/signifier primitives for action posture, readiness tone, evidence links, recovery actions, and sign-off requirements. Fund Ledger reconciliation detail and Report Pack handoff surfaces render those primitives through `WorkstationStatePanelControl`, and `scripts/wpf_finance_ux_checks.py` now guards the W4 signifier wiring. Focused validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (11 passed, 0 failed) and `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~WorkstationPresentationModelsTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false -maxcpucount:1 --logger "console;verbosity=normal"` (3 passed against the existing binary). Fresh WPF build/test validation remains blocked locally: shared and isolated `bin/obj` WPF builds repeatedly timed out or exited without compiler output after restore/MSBuild startup, so this is support evidence only until a clean WPF build-backed run completes.
- 2026-05-28: Lane C shared/browser implementation evidence is green after the W4 endpoint and workflow pass. Focused validation: `dotnet build tests/Meridian.Tests/Meridian.Tests.csproj --no-dependencies --no-restore /p:UseSharedCompilation=false /p:UseRoslynAnalyzers=false /p:RunAnalyzers=false /nr:false -v quiet` (build succeeded); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~OperationsContinuityWorkflowServiceTests|FullyQualifiedName~ReconciliationCaseWorkflowServiceTests|FullyQualifiedName~FundOperationsWorkspaceReadServiceTests"` (71 passed); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~ReconciliationGovernanceServiceTests|FullyQualifiedName~ReportPackWorkflowServiceTests|FullyQualifiedName~WorkstationEndpointsTests.MapWorkstationEndpoints_SecurityMaster"` (20 passed); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~FundStructureEndpointTests.PreviewReportPack|FullyQualifiedName~FundStructureEndpointTests.GenerateReportPack|FullyQualifiedName~FundStructureEndpointTests.GetReportPack|FullyQualifiedName~FundStructureEndpointTests.GetWorkspaceView|FullyQualifiedName~FundStructureEndpointTests.AccountReadiness|FullyQualifiedName~FundStructureEndpointTests.GetCashFlowView"` (16 passed); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~FundOpsCloseLaneScenarioTests|FullyQualifiedName~PilotAcceptanceHarnessTests"` (4 passed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/operations-continuity-screen.view-model.test.ts src/screens/operations-continuity-screen.test.tsx src/screens/governance-screen.view-model.test.ts src/screens/governance-screen.test.tsx src/screens/reporting-screen.view-model.test.ts` (104 passed); and `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (11 passed). This is not a full Lane C close claim until the fresh WPF build-backed test lane stops returning `LASTEXITCODE=-1`.
- 2026-05-28: Lane C browser/WPF route-parity support evidence now makes operations-continuity close targets explicit in the shared workflow library. `AccountingReviewOperationsContinuity` targets `OperationsContinuity`; `AccountingReviewCloseReadiness` targets `OperationsClose`; the browser command palette resolves both to `/accounting/operations-continuity`; WPF registers both as Fund Operations aliases and routes operations-continuity API work items back to the same desktop lane. Focused validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (12 passed, 0 failed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/components/meridian/command-palette.view-model.test.ts` (19 passed); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~WorkflowLibraryEndpointTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (8 passed); and `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-restore --filter "FullyQualifiedName~NavigationServiceTests|FullyQualifiedName~ShellRouteRegistryTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (64 passed). This closes an explicit drift guard only; full W4 acceptance still depends on the broader close/case/report/evidence workflow proof.
- 2026-05-28: Lane C Explain-the-Break browser/WPF parity support evidence now guards `ReconciliationBreakExplanationDto` through Contracts, the Ui.Shared provider-ledger producer, browser Governance detail, and WPF Fund Ledger guidance. Focused validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Contracts src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard` (13 passed, 0 failed); `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-restore --filter "FullyQualifiedName~FundLedgerViewModelTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (9 passed); `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-restore --filter "FullyQualifiedName~FundReconciliationWorkbenchServiceTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (3 passed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/governance-screen.view-model.test.ts` (39 passed); and `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~ProviderLedgerReconciliationServiceTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (27 passed). This is parity guard evidence, not full W4 close acceptance.
- 2026-05-28: Design-system semantic-state support evidence now makes the Ready/review/blocked/current/muted mapping explicit across browser badges, Evidence Workbench view models, WPF badge tone resources, design docs, screenshot docs, and screenshot quality gates. Focused validation: `python scripts/wpf_finance_ux_checks.py --root . --paths src/Meridian.Wpf src/Meridian.Ui.Shared src/Meridian.Ui/dashboard docs scripts` (13 passed, 0 failed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/design-system-contract.test.ts` (13 passed); and `python -m unittest tests.scripts.test_validate_screenshot_captures` (7 passed). This is an enforceable drift guard for retained evidence and operator-state consistency; it does not claim full W4 close, report-pack, or Evidence Vault acceptance.
- 2026-05-28: Lane C report-line provenance support evidence now requires retained report-pack line provenance to include the reported value plus at least one run, source-session, ledger-entry, reconciliation-case, or reconciliation-run pointer before governed publication. Focused validation: `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~ReportPackWorkflowServiceTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (9 passed after build-server shutdown; first attempt timed out before returning test output). This advances value-level report lineage, not full W4 close acceptance.
- 2026-05-28: Accounting-grade paper/Books Before Broker support evidence now includes a shared Investment Accounting Transaction Lab preview service and fund-structure endpoint for trades, dividends, accruals, fees, corporate actions, and broker reconciliation examples. The preview emits balanced expected-journal candidates, trial-balance impact, ledger-impact warnings, retained evidence ids, and reconciliation expectations from shared contracts. Focused validation: `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~InvestmentAccountingTransactionLabServiceTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (4 passed after clearing a stale `dotnet test`/`vstest.console` assembly lock). Product UI and persisted lab sessions remain open.
- 2026-05-28: Accounting-grade paper/Books Before Broker browser support evidence now wires the shared Investment Accounting Transaction Lab preview into the Accounting ledger workstream instead of holding preview state client-local. The browser posts a shared `InvestmentAccountingTransactionLabRequest` to `/api/fund-structure/accounting/transaction-lab/preview`, then displays returned expected journal count, ledger impact, reconciliation expectation, evidence ids, and trial-balance impact rows. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/lib/workstation-endpoints.test.ts src/screens/governance-screen.view-model.test.ts` (2 files passed, 56 tests passed); `npm --prefix src/Meridian.Ui/dashboard run build` (passed); `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~InvestmentAccountingTransactionLabServiceTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (4 passed); `git diff --check` (passed with line-ending warnings only); and `python build/scripts/docs/mark-stale-docs.py --write --summary` (23 stale entries, 0 report files changed). `python build/scripts/docs/validate-doc-hashes.py --summary` still fails with the known broad source/README hash drift, including the dashboard README.
- 2026-05-28: Report Restatement Tracker browser support evidence now carries shared report-pack workflow records in the fund-operations workspace reporting summary and renders restatement reason, approver, prior version, changed lines, and evidence links in the browser report-pack task panel. Focused validation: `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~FundOperationsWorkspaceReadServiceTests.GetWorkspaceAsync_WithReportPackWorkflowService_ReturnsRestatementRecordsInReportingSummary|FullyQualifiedName~ReportPackWorkflowServiceTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (10 passed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/reporting-screen.view-model.test.ts src/screens/reporting-screen.test.tsx` (2 files passed, 42 tests passed); and `npm --prefix src/Meridian.Ui/dashboard run build` (passed). This advances browser review UX for restatements; persisted workflow recovery and similar-restatement analytics remain open.
- 2026-05-28: Lane B browser run-comparison support evidence now projects the richer shared run compare/diff contract in Research: artifact completeness, compatibility warnings, engine/mode context, final-equity delta, drawdown delta, and Sharpe delta are visible in the comparison detail and diff summary. Focused validation: `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~StrategyRunReadServiceTests.GetRunComparisonDtosAsync|FullyQualifiedName~PilotAcceptanceHarnessTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (5 passed); `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/research-screen.view-model.test.ts src/screens/research-screen.test.tsx` (2 files passed, 45 tests passed after correcting drawdown-delta tone); and `npm --prefix src/Meridian.Ui/dashboard run build` (passed). This improves portfolio/run comparison evidence depth; broader Backtest Studio unification and full portfolio drill-in acceptance remain open.
- 2026-05-28: Lane B run-history coverage evidence now adds a view-model-owned Strategy run library summary for retained-run count, backtest/paper/live coverage, paper/live-adjacent lineage, and normalized engine coverage across Meridian-native and Lean-style runs. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/research-screen.view-model.test.ts src/screens/research-screen.test.tsx` (2 files passed, 46 tests passed) and `npm --prefix src/Meridian.Ui/dashboard run build` (passed). This advances the run-history-beyond-backtest acceptance surface; Backtest Studio orchestration and persisted run-launch workflows remain open.
- 2026-05-28: Lane B portfolio comparison evidence now adds a browser Portfolio run-linked equity comparison summary derived from existing shared run rows: selected run rank by P&L, best and weakest P&L, best Sharpe, and mode/engine coverage render before the run evidence detail panel. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/portfolio-screen.view-model.test.ts src/screens/portfolio-screen.test.tsx` (2 files passed, 44 tests passed) and `npm --prefix src/Meridian.Ui/dashboard run build` (passed). This advances portfolio comparison across strategy runs; attribution/drawdown/cash-flow drill-in visualization remains open.
- 2026-05-28: Lane B portfolio drill-in evidence now lets the browser Portfolio selected-run panel load shared attribution, equity-curve/drawdown, cash-flow, and fill-summary slices on demand, then summarizes realized/unrealized bridge, max drawdown/recovery/final equity, net cash flow, and trade-level commission evidence. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/portfolio-screen.view-model.test.ts src/screens/portfolio-screen.test.tsx` (2 files passed, 46 tests passed) and `npm --prefix src/Meridian.Ui/dashboard run build` (passed). This advances portfolio drill-ins; full attribution tables, cash-flow ladders, and trade-level evidence grids remain open.
- 2026-05-28: Screenshot/evidence automation now hardens retained web captures against wrong-route evidence by recording Playwright's actual post-navigation browser path in the web manifest and failing validation when it differs from the expected route path. The shared validator already rejects missing, unexpected, stale, blank, low-entropy, and wrong-page-tag captures across web and desktop. Focused validation: `python -m unittest tests.scripts.test_validate_screenshot_captures tests.scripts.test_refresh_screenshots_workflow` (14 passed) and `python scripts/dev/validate-screenshot-captures.py --help` (passed). This advances the evidence automation gate; live capture-run artifact review remains required before replacing durable screenshots.
- 2026-05-28: Backtest result unification support evidence now puts native Meridian and QuantConnect Lean results behind the same SDK canonical `BacktestResult` normalization seam. Native Backtest Studio results are stamped as full coverage, while Lean result ingestion normalizes summary metrics into `BacktestResult` with external run id, `SummaryOnly` coverage kind, and compatibility warnings that propagate through run comparison DTOs. Focused validation: `dotnet test tests/Meridian.Backtesting.Tests/Meridian.Backtesting.Tests.csproj --no-restore --filter "FullyQualifiedName~CanonicalBacktestResultNormalizerTests" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (2 passed after build-server shutdown) and `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-build --no-restore --filter "FullyQualifiedName~StrategyRunReadServiceTests.GetRunComparisonDtosAsync_WithLeanSummaryOnlyResult_SurfaceCanonicalCoverageWarnings" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (1 passed). Existing `LeanEndpointTests.IngestResults_ValidLeanResultsFile_ReturnsSuccess` currently fails before ingestion on missing test-host authentication services for the permission gate, so endpoint-level proof remains a fixture issue outside this normalization slice.
- 2026-05-28: Run diff tooling support evidence now carries strategy id/version, lineage relation, compatibility level, engine/mode context, artifact completeness, and warnings through the shared `StrategyRunDiff` contract and browser Research view model. Focused validation: `dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --no-restore --filter "FullyQualifiedName~WorkstationEndpointsTests.MapWorkstationEndpoints_DiffRuns_ShouldReturnPositionAndMetricDeltas" /p:EnableWindowsTargeting=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (1 passed after clearing stale .NET workers) and `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/screens/research-screen.view-model.test.ts` (23 passed). Strategy-aware launch/preflight and full Backtest Studio orchestration remain open.

- 2026-05-28: Shared workflow target parity now has direct browser and WPF guards. Browser exports the page-tag route catalog and tests the built-in target set used by shared workflows; command-palette workflow commands render shared action tones as visible Ready/Review/Blocked states instead of neutral route labels. WPF `ShellRouteRegistryTests` now loads `BuiltInWorkflowDefinitionProvider` and asserts every workflow entry/action target resolves to a desktop route, which caught and closed the missing `EvidenceWorkbench` route by aliasing it to Fund Audit Trail. Focused validation: `npm --prefix src/Meridian.Ui/dashboard run test:vitest -- src/lib/workspace.test.ts src/components/meridian/command-palette.view-model.test.ts` (29 passed); `dotnet build tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-restore /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /nr:false -m:1 --verbosity:minimal` (passed with existing warnings); and `dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --no-build --filter "FullyQualifiedName~ShellRouteRegistryTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true /p:UseSharedCompilation=false /nr:false -m:1 --logger "console;verbosity=minimal"` (10 passed). This is explicit drift-guard evidence; it is not a full browser/WPF acceptance claim for every workflow screen.

## Validation Command Patterns

Use the narrowest command that covers the changed scenario. Typical proof lanes are:

```powershell
dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --filter "FullyQualifiedName~Wave2PaperTradingCockpitAcceptanceTests|FullyQualifiedName~Wave2OperatorInboxAcceptanceTests|FullyQualifiedName~TradingOperatorReadinessServiceTests" --logger "console;verbosity=normal"
dotnet test tests/Meridian.Tests/Meridian.Tests.csproj --filter "FullyQualifiedName~PilotAcceptanceHarnessTests" --logger "console;verbosity=normal"
dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~TradingWorkspaceShellViewModelTests|FullyQualifiedName~TradingWorkspaceShellPageTests|FullyQualifiedName~MainPageUiWorkflowTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true --logger "console;verbosity=normal"
dotnet test tests/Meridian.Wpf.Tests/Meridian.Wpf.Tests.csproj --filter "FullyQualifiedName~FundLedgerViewModelTests|FullyQualifiedName~FundAccountsViewModelTests|FullyQualifiedName~CashFlowViewModelTests|FullyQualifiedName~AccountPortfolioViewModelTests" /p:EnableWindowsTargeting=true /p:EnableFullWpfBuild=true --logger "console;verbosity=normal"
```

Broaden only when shared DTOs, endpoint behavior, route metadata, source README contracts, or both browser and desktop consumers changed.

## Release Blocker Checklist

Before accepting any desktop workflow slice, confirm:

- The scenario names one W2, W3, or W4 acceptance row above.
- Shared endpoint/read-model behavior exists or is intentionally unchanged.
- WPF presents shared state and does not fork business logic.
- Browser parity is either verified or the mismatch is logged as a blocker.
- The matching pilot-readiness stage is green or has a blocker with owner, expected evidence, and target follow-up.
- The status/planning docs identify the evidence packet or explicitly say the claim is support evidence only.
