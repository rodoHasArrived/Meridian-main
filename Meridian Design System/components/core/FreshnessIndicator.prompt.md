FreshnessIndicator — live-data status primitive. A colored status dot plus an optional source name and last-seen readout, encoding the voice rule "name the system, the time, the evidence". Place beside any streaming value — quote feeds, provider links, run telemetry. Flat by default; only `connecting` carries a quiet pulse. `ConnectionDot` is the bare dot for inline use next to a number.
