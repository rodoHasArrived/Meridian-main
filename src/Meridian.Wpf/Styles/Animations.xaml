<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ========================================== -->
    <!-- DURATION CONSTANTS -->
    <!-- ========================================== -->

    <Duration x:Key="InstantDuration">0:0:0.05</Duration>
    <Duration x:Key="QuickDuration">0:0:0.15</Duration>
    <Duration x:Key="NormalDuration">0:0:0.25</Duration>
    <Duration x:Key="SlowDuration">0:0:0.4</Duration>
    <Duration x:Key="EnterDuration">0:0:0.3</Duration>
    <Duration x:Key="ExitDuration">0:0:0.2</Duration>

    <!-- ========================================== -->
    <!-- EASING FUNCTIONS -->
    <!-- ========================================== -->

    <!-- Standard easing for most animations -->
    <QuadraticEase x:Key="StandardEasing" EasingMode="EaseOut" />

    <!-- Deceleration easing for entering elements -->
    <CubicEase x:Key="DecelerateEasing" EasingMode="EaseOut" />

    <!-- Acceleration easing for exiting elements -->
    <CubicEase x:Key="AccelerateEasing" EasingMode="EaseIn" />

    <!-- Sharp easing for quick micro-interactions -->
    <QuadraticEase x:Key="SharpEasing" EasingMode="EaseInOut" />

    <!-- Bounce easing for playful interactions -->
    <BounceEase x:Key="BounceEasing" Bounces="2" Bounciness="4" EasingMode="EaseOut" />

    <!-- Elastic easing for spring-like effects -->
    <ElasticEase x:Key="ElasticEasing" Oscillations="1" Springiness="3" EasingMode="EaseOut" />

    <!-- ========================================== -->
    <!-- FADE ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Fade In -->
    <Storyboard x:Key="FadeInStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Fade Out -->
    <Storyboard x:Key="FadeOutStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="1" To="0"
            Duration="0:0:0.15">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ========================================== -->
    <!-- SLIDE ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Slide In From Right -->
    <Storyboard x:Key="SlideInFromRightStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
            From="50" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Slide In From Left -->
    <Storyboard x:Key="SlideInFromLeftStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
            From="-50" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Slide In From Bottom -->
    <Storyboard x:Key="SlideInFromBottomStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
            From="30" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- Slide In From Top -->
    <Storyboard x:Key="SlideInFromTopStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
            From="-30" To="0"
            Duration="0:0:0.3">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
    </Storyboard>

    <!-- ========================================== -->
    <!-- SCALE ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Scale In (Pop) -->
    <Storyboard x:Key="ScaleInStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
            From="0.9" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
            From="0.9" To="1"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.15" />
    </Storyboard>

    <!-- Scale Out -->
    <Storyboard x:Key="ScaleOutStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)"
            From="1" To="0.9"
            Duration="0:0:0.15">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)"
            From="1" To="0.9"
            Duration="0:0:0.15">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="1" To="0"
            Duration="0:0:0.15" />
    </Storyboard>

    <!-- ========================================== -->
    <!-- PULSE ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Pulse Animation (for live indicators) -->
    <Storyboard x:Key="PulseStoryboard" RepeatBehavior="Forever">
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)">
            <SplineDoubleKeyFrame KeyTime="0:0:0" Value="1" />
            <SplineDoubleKeyFrame KeyTime="0:0:0.5" Value="1.2" KeySpline="0.4,0,0.2,1" />
            <SplineDoubleKeyFrame KeyTime="0:0:1" Value="1" KeySpline="0.4,0,0.2,1" />
        </DoubleAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)">
            <SplineDoubleKeyFrame KeyTime="0:0:0" Value="1" />
            <SplineDoubleKeyFrame KeyTime="0:0:0.5" Value="1.2" KeySpline="0.4,0,0.2,1" />
            <SplineDoubleKeyFrame KeyTime="0:0:1" Value="1" KeySpline="0.4,0,0.2,1" />
        </DoubleAnimationUsingKeyFrames>
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="Opacity">
            <SplineDoubleKeyFrame KeyTime="0:0:0" Value="1" />
            <SplineDoubleKeyFrame KeyTime="0:0:0.5" Value="0.6" />
            <SplineDoubleKeyFrame KeyTime="0:0:1" Value="1" />
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <!-- Subtle Pulse (gentler) -->
    <Storyboard x:Key="SubtlePulseStoryboard" RepeatBehavior="Forever">
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="Opacity">
            <SplineDoubleKeyFrame KeyTime="0:0:0" Value="1" />
            <SplineDoubleKeyFrame KeyTime="0:0:0.75" Value="0.5" />
            <SplineDoubleKeyFrame KeyTime="0:0:1.5" Value="1" />
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <!-- ========================================== -->
    <!-- SHAKE ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Shake Animation (for error feedback) -->
    <Storyboard x:Key="ShakeStoryboard">
        <DoubleAnimationUsingKeyFrames
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)">
            <LinearDoubleKeyFrame KeyTime="0:0:0" Value="0" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.05" Value="-8" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.1" Value="8" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.15" Value="-6" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.2" Value="6" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.25" Value="-4" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.3" Value="4" />
            <LinearDoubleKeyFrame KeyTime="0:0:0.35" Value="0" />
        </DoubleAnimationUsingKeyFrames>
    </Storyboard>

    <!-- ========================================== -->
    <!-- ROTATION ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Spin Animation (for loading indicators) -->
    <Storyboard x:Key="SpinStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(RotateTransform.Angle)"
            From="0" To="360"
            Duration="0:0:1" />
    </Storyboard>

    <!-- ========================================== -->
    <!-- SKELETON LOADING ANIMATION -->
    <!-- ========================================== -->

    <!-- Skeleton Shimmer -->
    <Storyboard x:Key="SkeletonShimmerStoryboard" RepeatBehavior="Forever">
        <DoubleAnimation
            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
            From="-100" To="300"
            Duration="0:0:1.5" />
    </Storyboard>

    <!-- ========================================== -->
    <!-- HIGHLIGHT ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Flash Highlight (for new items) -->
    <Storyboard x:Key="FlashHighlightStoryboard">
        <ColorAnimationUsingKeyFrames
            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)">
            <LinearColorKeyFrame KeyTime="0:0:0" Value="#333fb950" />
            <LinearColorKeyFrame KeyTime="0:0:0.3" Value="#333fb950" />
            <SplineColorKeyFrame KeyTime="0:0:0.8" Value="Transparent" KeySpline="0.4,0,0.2,1" />
        </ColorAnimationUsingKeyFrames>
    </Storyboard>

    <!-- ========================================== -->
    <!-- ACCORDION/EXPAND ANIMATIONS -->
    <!-- ========================================== -->

    <!-- Expand Height -->
    <Storyboard x:Key="ExpandHeightStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(FrameworkElement.MaxHeight)"
            From="0" To="500"
            Duration="0:0:0.25">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseOut" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="0" To="1"
            Duration="0:0:0.15" />
    </Storyboard>

    <!-- Collapse Height -->
    <Storyboard x:Key="CollapseHeightStoryboard">
        <DoubleAnimation
            Storyboard.TargetProperty="(FrameworkElement.MaxHeight)"
            From="500" To="0"
            Duration="0:0:0.15">
            <DoubleAnimation.EasingFunction>
                <CubicEase EasingMode="EaseIn" />
            </DoubleAnimation.EasingFunction>
        </DoubleAnimation>
        <DoubleAnimation
            Storyboard.TargetProperty="Opacity"
            From="1" To="0"
            Duration="0:0:0.15" />
    </Storyboard>

</ResourceDictionary>
