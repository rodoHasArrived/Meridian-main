<UserControl x:Class="Meridian.Wpf.Workstation.Controls.ActivityLogGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">
    <StackPanel AutomationProperties.Name="{Binding ElementName=Root, Path=Timeline.Title}">
        <TextBlock Text="{Binding ElementName=Root, Path=Timeline.Title}"
                   Style="{StaticResource CardHeaderStyle}" />
        <ItemsControl ItemsSource="{Binding ElementName=Root, Path=Timeline.Entries}" Margin="0,10,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border BorderThickness="0,0,0,1"
                            BorderBrush="{StaticResource ConsoleBorderBrush}"
                            Padding="0,8">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <Ellipse Grid.Column="0"
                                     Width="8"
                                     Height="8"
                                     Fill="{StaticResource InfoColorBrush}"
                                     VerticalAlignment="Center"
                                     Margin="0,0,12,0" />
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="{Binding Title}"
                                           Foreground="{StaticResource ConsoleTextPrimaryBrush}" />
                                <TextBlock Text="{Binding Detail}"
                                           Foreground="{StaticResource ConsoleTextMutedBrush}"
                                           FontSize="12"
                                           TextWrapping="Wrap" />
                            </StackPanel>
                            <TextBlock Grid.Column="2"
                                       Text="{Binding TimeText}"
                                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                                       FontSize="12"
                                       VerticalAlignment="Center"
                                       Margin="12,0,0,0" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <TextBlock Text="{Binding ElementName=Root, Path=Timeline.EmptyText}"
                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                   Visibility="{Binding ElementName=Root, Path=IsTimelineEmpty, Converter={StaticResource BoolToVisibilityConverter}}" />
    </StackPanel>
</UserControl>
