<UserControl x:Class="Meridian.Wpf.Workstation.Controls.DenseDataGridControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="clr-namespace:Meridian.Wpf.Workstation.Controls"
             x:Name="Root">
    <UserControl.Resources>
        <Style x:Key="DenseGridColumnHeaderStyle" TargetType="{x:Type GridViewColumnHeader}">
            <Setter Property="Padding" Value="8,5" />
            <Setter Property="HorizontalContentAlignment" Value="Left" />
            <Setter Property="Background" Value="{StaticResource ConsoleBackgroundMediumBrush}" />
            <Setter Property="Foreground" Value="{StaticResource ConsoleTextMutedBrush}" />
            <Setter Property="FontSize" Value="10" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderBrush" Value="{StaticResource ConsoleBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,1,1" />
        </Style>

        <Style x:Key="DenseGridRowItemStyle" TargetType="{x:Type ListViewItem}">
            <Setter Property="MinHeight" Value="26" />
            <Setter Property="Padding" Value="4,1" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
            <Setter Property="Background" Value="{StaticResource ConsoleBackgroundLightBrush}" />
            <Setter Property="Foreground" Value="{StaticResource ConsoleTextPrimaryBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource ConsoleBorderBrush}" />
            <Setter Property="BorderThickness" Value="0,0,0,1" />
            <Setter Property="KeyboardNavigation.TabNavigation" Value="Local" />
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource ConsoleBackgroundHoverBrush}" />
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="{StaticResource ConsoleAccentBlueAlpha20Brush}" />
                    <Setter Property="Foreground" Value="{StaticResource ConsoleTextPrimaryBrush}" />
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource ConsoleBorderFocusBrush}" />
                    <Setter Property="BorderThickness" Value="1" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <Grid.InputBindings>
            <KeyBinding Key="F" Modifiers="Control" Command="{x:Static controls:DenseGridKeyboardCommands.FocusFilter}" />
            <KeyBinding Key="Enter" Command="{x:Static controls:DenseGridKeyboardCommands.OpenSelectedDetails}" />
            <KeyBinding Key="Escape" Command="{x:Static controls:DenseGridKeyboardCommands.CloseDetails}" />
            <KeyBinding Key="C" Modifiers="Control" Command="ApplicationCommands.Copy" />
            <KeyBinding Key="F" Modifiers="Control+Shift" Command="{x:Static controls:DenseGridKeyboardCommands.ClearFilters}" />
            <KeyBinding Key="J" Modifiers="Control" Command="{x:Static controls:DenseGridKeyboardCommands.JumpToRelatedRecords}" />
        </Grid.InputBindings>
        <ListView x:Name="RowsList"
                  ItemsSource="{Binding ElementName=Root, Path=Table.Rows}"
                  SelectedItem="{Binding ElementName=Root, Path=SelectedItem, Mode=TwoWay}"
                  MinHeight="220"
                  FontSize="11"
                  BorderThickness="1"
                  BorderBrush="{StaticResource ConsoleBorderBrush}"
                  Background="{StaticResource ConsoleBackgroundLightBrush}"
                  SelectionMode="{Binding ElementName=Root, Path=SelectionMode}"
                  ScrollViewer.CanContentScroll="True"
                  VirtualizingPanel.IsVirtualizing="True"
                  VirtualizingPanel.VirtualizationMode="Recycling"
                  VirtualizingPanel.ScrollUnit="Item"
                  KeyboardNavigation.TabNavigation="Local"
                  KeyboardNavigation.DirectionalNavigation="Contained"
                  ItemContainerStyle="{StaticResource DenseGridRowItemStyle}"
                  AutomationProperties.AutomationId="{Binding ElementName=Root, Path=GridAutomationId}"
                  AutomationProperties.Name="{Binding ElementName=Root, Path=Table.Title}"
                  SelectionChanged="RowsList_SelectionChanged" />
        <Border x:Name="EmptyPanel"
                Visibility="Collapsed"
                Background="{StaticResource PremiumPanelBrush}"
                BorderBrush="{StaticResource ConsoleBorderBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="18"
                HorizontalAlignment="Stretch"
                VerticalAlignment="Stretch"
                AutomationProperties.AutomationId="{Binding ElementName=Root, Path=EmptyAutomationId}">
            <Grid>
                <StackPanel x:Name="DefaultEmptyContent"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <StackPanel.Style>
                        <Style TargetType="{x:Type StackPanel}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=Root, Path=EmptyContent}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </StackPanel.Style>
                <TextBlock Text="{Binding ElementName=Root, Path=Table.EmptyTitle}"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                           HorizontalAlignment="Center" />
                <TextBlock Text="{Binding ElementName=Root, Path=Table.EmptyDetail}"
                           Margin="0,6,0,0"
                           Foreground="{StaticResource ConsoleTextMutedBrush}"
                           TextWrapping="Wrap"
                           HorizontalAlignment="Center" />
                </StackPanel>
                <ContentPresenter x:Name="EmptyContentPresenter"
                                  Content="{Binding ElementName=Root, Path=EmptyContent}">
                    <ContentPresenter.Style>
                        <Style TargetType="{x:Type ContentPresenter}">
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding ElementName=Root, Path=EmptyContent}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ContentPresenter.Style>
                </ContentPresenter>
            </Grid>
        </Border>
    </Grid>
</UserControl>
