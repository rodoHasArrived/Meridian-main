<UserControl x:Class="Meridian.Wpf.Controls.FreshnessIndicator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             x:Name="Root"
             mc:Ignorable="d"
             MinHeight="32"
             d:DesignHeight="44" d:DesignWidth="220"
             AutomationProperties.AutomationId="{Binding ElementName=Root, Path=IndicatorAutomationId}"
             AutomationProperties.Name="{Binding ElementName=Root, Path=Label}">
    <Border x:Name="IndicatorBorder"
            DataContext="{Binding ElementName=Root}"
            Style="{StaticResource WorkspaceToneBadgeStyle}"
            Padding="8,5"
            AutomationProperties.AutomationId="{Binding IndicatorAutomationId}"
            AutomationProperties.Name="{Binding Label}">
        <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
            <TextBlock x:Name="IconText"
                       Text="{Binding IconGlyph}"
                       FontFamily="{StaticResource SymbolThemeFontFamily}"
                       Margin="0,0,6,0"
                       VerticalAlignment="Center"
                       AutomationProperties.AutomationId="{Binding IconAutomationId}">
                <TextBlock.Style>
                    <Style TargetType="TextBlock" BasedOn="{StaticResource WorkspaceToneBadgeTextStyle}">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IconGlyph}" Value="{x:Null}">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding IconGlyph}" Value="">
                                <Setter Property="Visibility" Value="Collapsed" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
            <StackPanel>
                <WrapPanel>
                    <TextBlock x:Name="LabelText"
                               Text="{Binding Label}"
                               FontSize="10"
                               Foreground="{StaticResource ConsoleTextMutedBrush}"
                               VerticalAlignment="Center"
                               AutomationProperties.AutomationId="{Binding LabelAutomationId}" />
                    <TextBlock Text="  "
                               FontSize="10" />
                    <TextBlock x:Name="ValueText"
                               Text="{Binding Value}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               Style="{StaticResource WorkspaceToneBadgeTextStyle}"
                               VerticalAlignment="Center"
                               AutomationProperties.AutomationId="{Binding ValueAutomationId}" />
                </WrapPanel>
                <TextBlock x:Name="DetailText"
                           Text="{Binding Detail}"
                           Margin="0,2,0,0"
                           FontSize="10"
                           Foreground="{StaticResource ConsoleTextMutedBrush}"
                           TextWrapping="Wrap"
                           AutomationProperties.AutomationId="{Binding DetailAutomationId}">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Detail}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Detail}" Value="">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </StackPanel>
    </Border>
</UserControl>
