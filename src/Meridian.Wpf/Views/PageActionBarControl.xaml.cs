namespace Meridian.Wpf.Views;

/// <summary>
/// Code-behind for PageActionBarControl.
/// Minimal: only InitializeComponent() — no business logic.
/// </summary>
public partial class PageActionBarControl
{
    public PageActionBarControl()
    {
        InitializeComponent();
    }
}
