<Page x:Class="Meridian.Wpf.Views.QuantScriptPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:avalonEdit="http://icsharpcode.net/sharpdevelop/avalonedit"
      xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
      xmlns:models="clr-namespace:Meridian.Wpf.Models"
      xmlns:behaviors="clr-namespace:Meridian.Wpf.Behaviors"
      Loaded="OnPageLoaded"
      Unloaded="OnPageUnloaded">

  <Page.Resources>
    <BooleanToVisibilityConverter x:Key="BoolVis" />

    <behaviors:ParameterTemplateSelector x:Key="ParamSelector">
      <behaviors:ParameterTemplateSelector.NumericTemplate>
        <DataTemplate DataType="{x:Type models:ParameterViewModel}">
          <StackPanel Margin="0,3">
            <TextBlock FontSize="11"
                       Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                       Text="{Binding Label}"
                       ToolTip="{Binding Description}" />
            <TextBox Margin="0,4,0,0"
                     FontSize="11"
                     Padding="6,4"
                     Background="{StaticResource ConsoleBackgroundMediumBrush}"
                     BorderBrush="{StaticResource ConsoleBorderBrush}"
                     Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                     Text="{Binding RawValue, UpdateSourceTrigger=PropertyChanged}" />
          </StackPanel>
        </DataTemplate>
      </behaviors:ParameterTemplateSelector.NumericTemplate>
      <behaviors:ParameterTemplateSelector.TextTemplate>
        <DataTemplate DataType="{x:Type models:ParameterViewModel}">
          <StackPanel Margin="0,3">
            <TextBlock FontSize="11"
                       Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                       Text="{Binding Label}"
                       ToolTip="{Binding Description}" />
            <TextBox Margin="0,4,0,0"
                     FontSize="11"
                     Padding="6,4"
                     Background="{StaticResource ConsoleBackgroundMediumBrush}"
                     BorderBrush="{StaticResource ConsoleBorderBrush}"
                     Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                     Text="{Binding RawValue, UpdateSourceTrigger=PropertyChanged}" />
          </StackPanel>
        </DataTemplate>
      </behaviors:ParameterTemplateSelector.TextTemplate>
      <behaviors:ParameterTemplateSelector.BoolTemplate>
        <DataTemplate DataType="{x:Type models:ParameterViewModel}">
          <StackPanel Margin="0,3">
            <TextBlock FontSize="11"
                       Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                       Text="{Binding Label}"
                       ToolTip="{Binding Description}" />
            <CheckBox Margin="0,4,0,0"
                      VerticalAlignment="Center"
                      IsChecked="{Binding BooleanValue, Mode=TwoWay}" />
          </StackPanel>
        </DataTemplate>
      </behaviors:ParameterTemplateSelector.BoolTemplate>
    </behaviors:ParameterTemplateSelector>
  </Page.Resources>

  <!-- ══════════════════════════════════════════════════════════════════════════
       Root: two rows — top toolbar strip + vertical-split content area
       ══════════════════════════════════════════════════════════════════════════ -->
  <Grid Background="{StaticResource ConsoleBackgroundDarkBrush}">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto" />   <!-- top toolbar -->
      <RowDefinition Height="*" />      <!-- chart + editor content -->
    </Grid.RowDefinitions>

    <!-- ════════════════════════════════════════════════════════════════════════
         Row 0 — Toolbar: script selector / interval / date range / run controls
         ════════════════════════════════════════════════════════════════════════ -->
    <Border Grid.Row="0"
            Background="{StaticResource ConsoleBackgroundMediumBrush}"
            BorderBrush="{StaticResource ConsoleBorderBrush}"
            BorderThickness="0,0,0,1"
            Padding="10,7">
      <DockPanel LastChildFill="False">

        <!-- Document selector -->
        <ComboBox DockPanel.Dock="Left"
                  Width="180"
                  Margin="0,0,8,0"
                  VerticalContentAlignment="Center"
                  Background="{StaticResource ConsoleBackgroundLightBrush}"
                  BorderBrush="{StaticResource ConsoleBorderBrush}"
                  Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                  ItemsSource="{Binding Documents}"
                  SelectedItem="{Binding SelectedDocument}">
          <ComboBox.ItemTemplate>
            <DataTemplate DataType="{x:Type models:ScriptDocumentEntry}">
              <StackPanel Orientation="Horizontal">
                <TextBlock FontSize="11" Text="{Binding Name}" />
                <TextBlock Margin="6,0,0,0"
                           FontSize="10"
                           Foreground="{StaticResource ConsoleTextMutedBrush}"
                           Text="{Binding KindLabel}" />
              </StackPanel>
            </DataTemplate>
          </ComboBox.ItemTemplate>
        </ComboBox>

        <!-- Interval -->
        <ComboBox DockPanel.Dock="Left"
                  Width="130"
                  Margin="0,0,8,0"
                  VerticalContentAlignment="Center"
                  Background="{StaticResource ConsoleBackgroundLightBrush}"
                  BorderBrush="{StaticResource ConsoleBorderBrush}"
                  FontSize="11"
                  Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                  ItemsSource="{Binding Intervals}"
                  SelectedItem="{Binding SelectedInterval}" />

        <!-- From date -->
        <Border DockPanel.Dock="Left"
                Margin="0,0,8,0"
                Padding="8,4"
                Background="{StaticResource ConsoleBackgroundLightBrush}"
                BorderBrush="{StaticResource ConsoleBorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
          <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0,0,4,0"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       Text="&#128197;" />
            <DatePicker Width="110"
                        FontSize="11"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                        SelectedDate="{Binding FromDate}" />
          </StackPanel>
        </Border>

        <!-- To date -->
        <Border DockPanel.Dock="Left"
                Margin="0,0,12,0"
                Padding="8,4"
                Background="{StaticResource ConsoleBackgroundLightBrush}"
                BorderBrush="{StaticResource ConsoleBorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
          <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0,0,4,0"
                       VerticalAlignment="Center"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       Text="&#128197;" />
            <DatePicker Width="110"
                        FontSize="11"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                        SelectedDate="{Binding ToDate}" />
          </StackPanel>
        </Border>

        <!-- Run -->
        <Button DockPanel.Dock="Left"
                Margin="0,0,6,0"
                Padding="16,6"
                Command="{Binding RunScriptCommand}"
                IsEnabled="{Binding CanRun}"
                Style="{StaticResource PrimaryButtonStyle}">
          <StackPanel Orientation="Horizontal">
            <TextBlock Margin="0,0,4,0"
                       FontSize="11"
                       Text="&#9654;" />
            <TextBlock FontSize="11" Text="Run" />
          </StackPanel>
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="0,0,6,0"
                Padding="12,6"
                Command="{Binding RunAllCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Run All" />
        </Button>

        <!-- Stop -->
        <Button DockPanel.Dock="Left"
                Margin="0,0,6,0"
                Padding="12,6"
                Command="{Binding StopCommand}"
                IsEnabled="{Binding IsRunning}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Stop" />
        </Button>

        <Separator DockPanel.Dock="Left" Margin="4,0" />

        <Button DockPanel.Dock="Left"
                Margin="0,0,4,0"
                Padding="10,6"
                Command="{Binding NewScriptCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="New" />
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="0,0,4,0"
                Padding="10,6"
                Command="{Binding NewNotebookCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Notebook" />
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="0,0,4,0"
                Padding="10,6"
                Command="{Binding SaveScriptCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Save" />
        </Button>

        <Button DockPanel.Dock="Left"
                Padding="10,6"
                Command="{Binding RefreshScriptsCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Refresh" />
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="4,0,0,0"
                Padding="10,6"
                Command="{Binding TemplatesCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Templates" />
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="4,0,0,0"
                Padding="10,6"
                Command="{Binding AddCellCommand}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Add Cell" />
        </Button>

        <Button DockPanel.Dock="Left"
                Margin="4,0,0,0"
                Padding="10,6"
                Command="{Binding DeleteCellCommand}"
                IsEnabled="{Binding CanDeleteCell}"
                Style="{StaticResource GhostButtonStyle}">
          <TextBlock FontSize="11" Text="Delete Cell" />
        </Button>

        <!-- Right-anchored status strip -->
        <ProgressBar DockPanel.Dock="Right"
                     Width="100"
                     Height="6"
                     Margin="8,0,0,0"
                     VerticalAlignment="Center"
                     IsIndeterminate="{Binding IsRunning}"
                     Maximum="1"
                     Minimum="0"
                     Value="{Binding ProgressFraction, Mode=OneWay}"
                     Visibility="{Binding IsRunning, Converter={StaticResource BoolVis}}" />
        <TextBlock DockPanel.Dock="Right"
                   Margin="8,0,0,0"
                   VerticalAlignment="Center"
                   FontSize="11"
                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                   Text="{Binding MemoryText}" />
        <TextBlock DockPanel.Dock="Right"
                   Margin="8,0,0,0"
                   VerticalAlignment="Center"
                   FontSize="11"
                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                   Text="{Binding ElapsedText}" />
        <TextBlock DockPanel.Dock="Right"
                   Margin="8,0,0,0"
                   VerticalAlignment="Center"
                   FontSize="11"
                   Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                   Text="{Binding StatusText}" />
        <TextBlock DockPanel.Dock="Right"
                   Margin="8,0,0,0"
                   VerticalAlignment="Center"
                   FontSize="11"
                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                   Text="{Binding CurrentDocumentKindText}" />

      </DockPanel>
    </Border>

    <!-- ════════════════════════════════════════════════════════════════════════
         Row 1 — Main content: vertical split (chart on top, editor tabs on bottom)
         ════════════════════════════════════════════════════════════════════════ -->
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition x:Name="ChartRow" Height="300*" MinHeight="80" />
        <RowDefinition Height="5" />
        <RowDefinition x:Name="EditorRow" Height="280*" MinHeight="100" />
      </Grid.RowDefinitions>

      <!-- ──────────────────────────────────────────────────────────────────────
           Chart area: asset input → chart title → ScottPlot → legend strip
           ──────────────────────────────────────────────────────────────────── -->
      <Grid Grid.Row="0">
        <Grid.RowDefinitions>
          <RowDefinition Height="Auto" />   <!-- asset input -->
          <RowDefinition Height="*" />      <!-- chart + empty state -->
          <RowDefinition Height="Auto" />   <!-- legend strip -->
        </Grid.RowDefinitions>

        <!-- Asset input row -->
        <Border Grid.Row="0"
                Padding="12,8"
                BorderBrush="{StaticResource ConsoleBorderBrush}"
                BorderThickness="0,0,0,1">
          <StackPanel Orientation="Horizontal">
            <StackPanel Margin="0,0,24,0">
              <TextBlock Margin="0,0,0,3"
                         FontSize="10"
                         Foreground="{StaticResource ConsoleTextMutedBrush}"
                         Text="Asset" />
              <Border Padding="8,4"
                      Background="{StaticResource ConsoleBackgroundLightBrush}"
                      BorderBrush="{StaticResource ConsoleBorderBrush}"
                      BorderThickness="1"
                      CornerRadius="4">
                <TextBox MinWidth="80"
                         FontSize="12"
                         Background="Transparent"
                         BorderThickness="0"
                         Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                         Text="{Binding AssetSymbol, UpdateSourceTrigger=PropertyChanged}" />
              </Border>
            </StackPanel>
          </StackPanel>
        </Border>

        <!-- Chart / empty-state area -->
        <Grid Grid.Row="1">

          <!-- Empty state placeholder when no chart result exists -->
          <StackPanel HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Visibility="{Binding HasNoChart, Converter={StaticResource BoolVis}}">
            <TextBlock HorizontalAlignment="Center"
                       FontSize="28"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       Text="&#128202;" />
            <TextBlock Margin="0,8,0,0"
                       HorizontalAlignment="Center"
                       FontSize="13"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       Text="Run a script to see results" />
            <TextBlock Margin="0,4,0,0"
                       HorizontalAlignment="Center"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextDisabledBrush}"
                       Text="Charts, metrics and trade output will appear here" />
          </StackPanel>

          <!-- Chart title (top-left overlay) -->
          <StackPanel Margin="12,8,0,0"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Top"
                      Visibility="{Binding HasChart, Converter={StaticResource BoolVis}}">
            <TextBlock FontSize="14"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                       Text="{Binding PrimaryChartTitle}" />
            <TextBlock Margin="0,2,0,0"
                       FontSize="10"
                       Foreground="{StaticResource ConsoleTextMutedBrush}"
                       Text="{Binding FromDate, StringFormat={}{0:d MMM yy}}" />
          </StackPanel>

          <!-- ScottPlot chart -->
          <scottplot:WpfPlot behaviors:PlotRenderBehavior.Request="{Binding PrimaryChartRequest}"
                             Visibility="{Binding HasChart, Converter={StaticResource BoolVis}}" />
        </Grid>

        <!-- Legend strip -->
        <Border Grid.Row="2"
                Padding="12,6"
                Background="{StaticResource ConsoleBackgroundMediumBrush}"
                BorderBrush="{StaticResource ConsoleBorderBrush}"
                BorderThickness="0,1,0,0"
                Visibility="{Binding HasLegend, Converter={StaticResource BoolVis}}">
          <ItemsControl ItemsSource="{Binding LegendEntries}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <StackPanel Orientation="Horizontal" />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate DataType="{x:Type models:ChartLegendEntry}">
                <StackPanel Margin="0,0,16,0" Orientation="Horizontal">
                  <Rectangle Width="12"
                             Height="12"
                             Margin="0,0,5,0"
                             VerticalAlignment="Center"
                             Fill="{Binding SeriesColorBrush}" />
                  <TextBlock VerticalAlignment="Center"
                             FontSize="11"
                             Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                             Text="{Binding Label}" />
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </Border>
      </Grid>

      <!-- GridSplitter -->
      <GridSplitter Grid.Row="1"
                    Height="5"
                    HorizontalAlignment="Stretch"
                    Background="{StaticResource ConsoleBorderBrush}"
                    ResizeDirection="Rows" />

      <!-- ──────────────────────────────────────────────────────────────────────
           Editor tab area — Expression Editor is tab 0 (matches Marquee UX)
           ──────────────────────────────────────────────────────────────────── -->
      <TabControl Grid.Row="2"
                  Background="{StaticResource ConsoleBackgroundDarkBrush}"
                  BorderBrush="{StaticResource ConsoleBorderBrush}"
                  Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                  SelectedIndex="{Binding ActiveResultsTab}">

        <!-- Tab 0: Expression Editor -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12"
                       FontWeight="SemiBold"
                       Foreground="{StaticResource InfoColorBrush}"
                       Text="Expression Editor" />
          </TabItem.Header>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <avalonEdit:TextEditor x:Name="ScriptEditor"
                                   Grid.Row="0"
                                   Background="#0D1117"
                                   behaviors:AvalonEditNotebookBehavior.Text="{Binding ScriptSource, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                   behaviors:AvalonEditNotebookBehavior.RunCommand="{Binding RunScriptCommand}"
                                   behaviors:AvalonEditNotebookBehavior.RunAndAdvanceCommand="{Binding RunAndAdvanceCommand}"
                                   FontFamily="Cascadia Code, Consolas, Courier New"
                                   FontSize="13"
                                   Foreground="#E6EDF3"
                                   ShowLineNumbers="True"
                                   SyntaxHighlighting="C#"
                                   WordWrap="False" />

            <!-- Status strip for editor -->
            <Border Grid.Row="1"
                    Padding="10,4"
                    Background="{StaticResource ConsoleBackgroundMediumBrush}"
                    BorderBrush="{StaticResource ConsoleBorderBrush}"
                    BorderThickness="0,1,0,0">
              <DockPanel LastChildFill="False">
                <Button DockPanel.Dock="Right"
                        Margin="6,0,0,0"
                        Padding="8,3"
                        Command="{Binding ClearConsoleCommand}"
                        Style="{StaticResource GhostButtonStyle}">
                  <TextBlock FontSize="10" Text="Clear" />
                </Button>
                <TextBlock VerticalAlignment="Center"
                           FontSize="10"
                           Foreground="{StaticResource ConsoleTextMutedBrush}"
                           Text="{Binding StatusText}" />
              </DockPanel>
            </Border>
          </Grid>
        </TabItem>

        <!-- Tab 1: Data Sheet (Metrics) -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12" Text="{Binding MetricsTabHeader}" />
          </TabItem.Header>
          <DataGrid AutoGenerateColumns="False"
                    Background="{StaticResource ConsoleBackgroundDarkBrush}"
                    BorderThickness="0"
                    FontSize="12"
                    Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                    GridLinesVisibility="None"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    ItemsSource="{Binding Metrics}"
                    RowBackground="{StaticResource ConsoleBackgroundDarkBrush}">
            <DataGrid.Columns>
              <DataGridTextColumn Width="180" Binding="{Binding Label}" Header="Metric" />
              <DataGridTextColumn Width="*" Binding="{Binding Value}" Header="Value" />
              <DataGridTextColumn Width="120" Binding="{Binding Category}" Header="Category" />
            </DataGrid.Columns>
          </DataGrid>
        </TabItem>

        <!-- Tab 2: Statistics (Diagnostics) -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12" Text="{Binding DiagnosticsTabHeader}" />
          </TabItem.Header>
          <DataGrid AutoGenerateColumns="False"
                    Background="{StaticResource ConsoleBackgroundDarkBrush}"
                    BorderThickness="0"
                    FontSize="12"
                    Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                    GridLinesVisibility="None"
                    HeadersVisibility="Column"
                    IsReadOnly="True"
                    ItemsSource="{Binding Diagnostics}"
                    RowBackground="{StaticResource ConsoleBackgroundDarkBrush}">
            <DataGrid.Columns>
              <DataGridTextColumn Width="180" Binding="{Binding Key}" Header="Key" />
              <DataGridTextColumn Width="*" Binding="{Binding Value}" Header="Value" />
            </DataGrid.Columns>
          </DataGrid>
        </TabItem>

        <!-- Tab 3: Local Data (Scripts browser + Parameters) -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12" Text="Local Data" />
          </TabItem.Header>
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="220" />
              <ColumnDefinition Width="8" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="180" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <TextBlock Grid.Row="0"
                         Margin="0,0,0,6"
                         FontSize="11"
                         FontWeight="SemiBold"
                         Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                         Text="TEMPLATES" />
              <ListBox Grid.Row="1"
                       Background="{StaticResource ConsoleBackgroundMediumBrush}"
                       BorderBrush="{StaticResource ConsoleBorderBrush}"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                       ItemsSource="{Binding Templates}"
                       SelectedItem="{Binding SelectedTemplate}">
                <ListBox.ItemTemplate>
                  <DataTemplate DataType="{x:Type models:QuantScriptTemplateDefinition}">
                    <StackPanel Margin="4,3">
                      <TextBlock FontWeight="SemiBold" Text="{Binding Title}" />
                      <TextBlock Margin="0,2,0,0"
                                 FontSize="10"
                                 Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                                 Text="{Binding Description}"
                                 TextWrapping="Wrap" />
                      <TextBlock Margin="0,2,0,0"
                                 FontSize="10"
                                 Foreground="{StaticResource ConsoleTextMutedBrush}"
                                 Text="{Binding Category}" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>

              <TextBlock Grid.Row="2"
                         Margin="0,10,0,6"
                         FontSize="11"
                         FontWeight="SemiBold"
                         Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                         Text="DOCUMENTS" />
              <ListBox Grid.Row="3"
                       Background="{StaticResource ConsoleBackgroundMediumBrush}"
                       BorderBrush="{StaticResource ConsoleBorderBrush}"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                       ItemsSource="{Binding Documents}"
                       SelectedItem="{Binding SelectedDocument}">
                <ListBox.ItemTemplate>
                  <DataTemplate DataType="{x:Type models:ScriptDocumentEntry}">
                    <StackPanel Margin="4,3">
                      <TextBlock Text="{Binding Name}" ToolTip="{Binding FullPath}" />
                      <TextBlock FontSize="10"
                                 Foreground="{StaticResource ConsoleTextMutedBrush}"
                                 Text="{Binding KindLabel}" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>
            </Grid>

            <Grid Grid.Column="2">
              <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="140" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
              </Grid.RowDefinitions>

              <TextBlock Grid.Row="0"
                         Margin="0,0,0,6"
                         FontSize="11"
                         FontWeight="SemiBold"
                         Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                         Text="CELLS" />
              <ListBox Grid.Row="1"
                       Background="{StaticResource ConsoleBackgroundMediumBrush}"
                       BorderBrush="{StaticResource ConsoleBorderBrush}"
                       FontSize="11"
                       Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                       ItemsSource="{Binding NotebookCells}"
                       SelectedItem="{Binding SelectedCell}">
                <ListBox.ItemTemplate>
                  <DataTemplate>
                    <StackPanel Margin="4,3">
                      <DockPanel>
                        <TextBlock DockPanel.Dock="Left"
                                   FontWeight="SemiBold"
                                   Text="{Binding Title}" />
                        <TextBlock DockPanel.Dock="Right"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                                   Text="{Binding StatusText}" />
                      </DockPanel>
                      <TextBlock Margin="0,2,0,0"
                                 Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                                 Text="{Binding Preview}"
                                 TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                  </DataTemplate>
                </ListBox.ItemTemplate>
              </ListBox>

              <TextBlock Grid.Row="2"
                         Margin="0,10,0,6"
                         FontSize="11"
                         FontWeight="SemiBold"
                         Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                         Text="PARAMETERS" />
              <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding Parameters}"
                              ItemTemplateSelector="{StaticResource ParamSelector}" />
              </ScrollViewer>
            </Grid>
          </Grid>
        </TabItem>

        <!-- Tab 4: Backtest Output (Trades + Console) -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12" Text="{Binding TradesTabHeader}" />
          </TabItem.Header>
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="*" />
              <RowDefinition Height="5" />
              <RowDefinition Height="120" MinHeight="60" />
            </Grid.RowDefinitions>

            <DataGrid Grid.Row="0"
                      AutoGenerateColumns="False"
                      Background="{StaticResource ConsoleBackgroundDarkBrush}"
                      BorderThickness="0"
                      FontSize="12"
                      Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      ItemsSource="{Binding Trades}"
                      RowBackground="{StaticResource ConsoleBackgroundDarkBrush}">
              <DataGrid.Columns>
                <DataGridTextColumn Width="140" Binding="{Binding FilledAt}" Header="Filled At" />
                <DataGridTextColumn Width="90" Binding="{Binding Symbol}" Header="Symbol" />
                <DataGridTextColumn Width="90" Binding="{Binding FilledQuantity}" Header="Qty" />
                <DataGridTextColumn Width="100" Binding="{Binding FillPrice}" Header="Price" />
                <DataGridTextColumn Width="100" Binding="{Binding Commission}" Header="Commission" />
                <DataGridTextColumn Width="80" Binding="{Binding Side}" Header="Side" />
              </DataGrid.Columns>
            </DataGrid>

            <GridSplitter Grid.Row="1"
                          Height="5"
                          HorizontalAlignment="Stretch"
                          Background="{StaticResource ConsoleBorderBrush}"
                          ResizeDirection="Rows" />

            <!-- Console log strip at bottom of Backtest Output tab -->
            <ScrollViewer x:Name="ConsoleScroll"
                          Grid.Row="2"
                          Background="{StaticResource ConsoleBackgroundMediumBrush}"
                          VerticalScrollBarVisibility="Auto">
              <ItemsControl ItemsSource="{Binding ConsoleOutput}">
                <ItemsControl.ItemTemplate>
                  <DataTemplate DataType="{x:Type models:ConsoleEntry}">
                    <TextBlock Padding="8,1"
                               FontFamily="Cascadia Code, Consolas"
                               FontSize="11"
                               Text="{Binding Text}"
                               TextWrapping="Wrap">
                      <TextBlock.Style>
                        <Style TargetType="TextBlock">
                          <Setter Property="Foreground" Value="{StaticResource ConsoleTextSecondaryBrush}" />
                          <Style.Triggers>
                            <DataTrigger Binding="{Binding Kind}" Value="Error">
                              <Setter Property="Foreground" Value="{StaticResource ErrorColorBrush}" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding Kind}" Value="Warning">
                              <Setter Property="Foreground" Value="{StaticResource WarningColorBrush}" />
                            </DataTrigger>
                          </Style.Triggers>
                        </Style>
                      </TextBlock.Style>
                    </TextBlock>
                  </DataTemplate>
                </ItemsControl.ItemTemplate>
              </ItemsControl>
            </ScrollViewer>
          </Grid>
        </TabItem>

        <!-- Tab 5: Run History -->
        <TabItem>
          <TabItem.Header>
            <TextBlock FontSize="12" Text="{Binding RunHistoryTabHeader}" />
          </TabItem.Header>
          <Grid Margin="8">
            <Grid.ColumnDefinitions>
              <ColumnDefinition Width="2*" />
              <ColumnDefinition Width="12" />
              <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <DataGrid Grid.Column="0"
                      AutoGenerateColumns="False"
                      Background="{StaticResource ConsoleBackgroundDarkBrush}"
                      BorderThickness="0"
                      FontSize="12"
                      Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                      GridLinesVisibility="None"
                      HeadersVisibility="Column"
                      IsReadOnly="True"
                      ItemsSource="{Binding RunHistory}"
                      RowBackground="{StaticResource ConsoleBackgroundDarkBrush}"
                      SelectedItem="{Binding SelectedExecutionRecord}">
              <DataGrid.Columns>
                <DataGridTextColumn Width="140" Binding="{Binding ExecutedAtText}" Header="Executed" />
                <DataGridTextColumn Width="160" Binding="{Binding DocumentTitle}" Header="Document" />
                <DataGridTextColumn Width="90" Binding="{Binding StatusText}" Header="Status" />
                <DataGridTextColumn Width="90" Binding="{Binding DocumentKindLabel}" Header="Kind" />
                <DataGridTextColumn Width="90" Binding="{Binding CapturedBacktestCount}" Header="Backtests" />
                <DataGridTextColumn Width="*" Binding="{Binding MirroredRunLabel}" Header="Research Run" />
              </DataGrid.Columns>
            </DataGrid>

            <Border Grid.Column="2"
                    Padding="12"
                    Background="{StaticResource ConsoleBackgroundMediumBrush}"
                    BorderBrush="{StaticResource ConsoleBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6">
              <Grid>
                <Grid.RowDefinitions>
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="Auto" />
                  <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           FontSize="14"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                           Text="{Binding SelectedExecutionRecord.DocumentTitle}" />

                <WrapPanel Grid.Row="1" Margin="0,10,0,0">
                  <Button Margin="0,0,8,0"
                          Padding="10,6"
                          Command="{Binding OpenRunBrowserCommand}"
                          Style="{StaticResource GhostButtonStyle}">
                    <TextBlock FontSize="11" Text="Open Run Browser" />
                  </Button>
                  <Button Margin="0,0,8,0"
                          Padding="10,6"
                          Command="{Binding OpenRunDetailCommand}"
                          Style="{StaticResource GhostButtonStyle}">
                    <TextBlock FontSize="11" Text="Open Run Detail" />
                  </Button>
                  <Button Padding="10,6"
                          Command="{Binding CompareInResearchCommand}"
                          Style="{StaticResource GhostButtonStyle}">
                    <TextBlock FontSize="11" Text="Compare in Research" />
                  </Button>
                </WrapPanel>

                <Grid Grid.Row="2" Margin="0,14,0,0">
                  <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                  </Grid.ColumnDefinitions>
                  <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                  </Grid.RowDefinitions>

                  <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,10,4" Foreground="{StaticResource ConsoleTextMutedBrush}" Text="Executed" />
                  <TextBlock Grid.Row="0" Grid.Column="1" Margin="0,0,0,4" Text="{Binding SelectedExecutionRecord.ExecutedAtText}" />

                  <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,10,4" Foreground="{StaticResource ConsoleTextMutedBrush}" Text="Status" />
                  <TextBlock Grid.Row="1" Grid.Column="1" Margin="0,0,0,4" Text="{Binding SelectedExecutionRecord.StatusText}" />

                  <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,10,4" Foreground="{StaticResource ConsoleTextMutedBrush}" Text="Parameters" />
                  <TextBlock Grid.Row="2" Grid.Column="1" Margin="0,0,0,4" Text="{Binding SelectedExecutionRecord.ParameterSummary}" TextWrapping="Wrap" />

                  <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,10,0" Foreground="{StaticResource ConsoleTextMutedBrush}" Text="Research Run" />
                  <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding SelectedExecutionRecord.MirroredRunLabel}" TextWrapping="Wrap" />
                </Grid>

                <Border Grid.Row="3"
                        Margin="0,14,0,0"
                        Padding="8"
                        Background="{StaticResource ConsoleBackgroundLightBrush}"
                        BorderBrush="{StaticResource ConsoleBorderBrush}"
                        BorderThickness="1"
                        CornerRadius="4">
                  <TextBlock Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                             Text="{Binding SelectedExecutionRecord.ConsolePreview}"
                             TextWrapping="Wrap" />
                </Border>

                <TextBlock Grid.Row="4"
                           Margin="0,12,0,6"
                           FontSize="11"
                           FontWeight="SemiBold"
                           Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                           Text="METRICS" />

                <ItemsControl Grid.Row="5"
                              ItemsSource="{Binding SelectedExecutionRecord.Metrics}">
                  <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type models:QuantScriptExecutionMetricRecord}">
                      <DockPanel Margin="0,0,0,4">
                        <TextBlock DockPanel.Dock="Left"
                                   Margin="0,0,10,0"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                                   Text="{Binding Label}" />
                        <TextBlock DockPanel.Dock="Right"
                                   Foreground="{StaticResource ConsoleTextPrimaryBrush}"
                                   Text="{Binding Value}" />
                      </DockPanel>
                    </DataTemplate>
                  </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Grid.Row="6"
                           Margin="0,12,0,0"
                           Foreground="{StaticResource WarningColorBrush}"
                           Text="{Binding SelectedExecutionRecord.Warning}"
                           TextWrapping="Wrap" />
              </Grid>
            </Border>
          </Grid>
        </TabItem>

      </TabControl>
    </Grid>
  </Grid>
</Page>
