<Page x:Class="Meridian.Wpf.Views.QuantScriptPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:avalonedit="http://icsharpcode.net/sharpdevelop/avalonedit"
      xmlns:local="clr-namespace:Meridian.Wpf.Views"
      Title="QuantScript"
      Background="{DynamicResource ShellWindowBackgroundBrush}"
      AutomationProperties.Name="QuantScript">

  <Page.Resources>
    <local:ConsoleKindToBrushConverter x:Key="ConsoleColorConverter"/>
    <BooleanToVisibilityConverter x:Key="BoolToVis"/>
  </Page.Resources>

  <!-- Three-column grid: script browser | editor | results -->
  <Grid>
    <Grid.ColumnDefinitions>
      <ColumnDefinition Width="180" MinWidth="120"/>
      <ColumnDefinition Width="4"/>
      <ColumnDefinition Width="*" MinWidth="200"/>
      <ColumnDefinition Width="4"/>
      <ColumnDefinition Width="300" MinWidth="200"/>
    </Grid.ColumnDefinitions>

    <!-- Col 0: Script browser -->
    <DockPanel Grid.Column="0">
      <ToolBar DockPanel.Dock="Top">
        <Button Command="{Binding NewScriptCommand}" ToolTip="New script">New</Button>
        <Button Command="{Binding SaveScriptCommand}" ToolTip="Save script">Save</Button>
        <Button Command="{Binding RefreshScriptsCommand}" ToolTip="Refresh list">↻</Button>
      </ToolBar>
      <ListBox ItemsSource="{Binding ScriptFiles}"
               SelectedItem="{Binding SelectedScript}"
               DisplayMemberPath="Name"/>
    </DockPanel>

    <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch"/>

    <!-- Col 2: Editor + parameters + run controls -->
    <DockPanel Grid.Column="2">

      <!-- Top toolbar: Run / Stop + status -->
      <ToolBar DockPanel.Dock="Top">
        <Button Command="{Binding RunCommand}"
                FontWeight="SemiBold" Padding="12,4">▶ Run</Button>
        <Button Command="{Binding StopCommand}"
                Margin="4,0,0,0">■ Stop</Button>
        <Separator/>
        <TextBlock Text="{Binding StatusText}"
                   VerticalAlignment="Center" Margin="4,0"/>
      </ToolBar>

      <!-- Parameters strip (shown only when parameters exist) -->
      <ItemsControl DockPanel.Dock="Bottom"
                    ItemsSource="{Binding Parameters}">
        <ItemsControl.ItemsPanel>
          <ItemsPanelTemplate>
            <WrapPanel/>
          </ItemsPanelTemplate>
        </ItemsControl.ItemsPanel>
        <ItemsControl.ItemTemplate>
          <DataTemplate>
            <StackPanel Orientation="Horizontal" Margin="8,4">
              <TextBlock Text="{Binding Descriptor.Label}"
                         VerticalAlignment="Center" Margin="0,0,4,0"
                         FontSize="11"/>
              <TextBox Text="{Binding RawValue, UpdateSourceTrigger=PropertyChanged}"
                       Width="80" Padding="4,2"/>
            </StackPanel>
          </DataTemplate>
        </ItemsControl.ItemTemplate>
      </ItemsControl>

      <!-- AvalonEdit -->
      <avalonedit:TextEditor x:Name="ScriptEditor"
                             SyntaxHighlighting="C#"
                             ShowLineNumbers="True"
                             FontFamily="Cascadia Code, Consolas, Monospace"
                             FontSize="13"/>
    </DockPanel>

    <GridSplitter Grid.Column="3" HorizontalAlignment="Stretch"/>

    <!-- Col 4: Results TabControl -->
    <TabControl Grid.Column="4">

      <TabItem Header="Console">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <ItemsControl ItemsSource="{Binding ConsoleOutput}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <TextBlock Text="{Binding Text}"
                           Foreground="{Binding Kind, Converter={StaticResource ConsoleColorConverter}}"
                           FontFamily="Cascadia Code, Consolas"
                           FontSize="11"
                           TextWrapping="Wrap"/>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Charts">
        <ScrollViewer VerticalScrollBarVisibility="Auto">
          <ItemsControl x:Name="ChartsPanel" ItemsSource="{Binding Charts}">
            <ItemsControl.ItemTemplate>
              <DataTemplate>
                <StackPanel Margin="4">
                  <TextBlock Text="{Binding Title}" FontWeight="SemiBold" Margin="0,4,0,2"/>
                  <!-- ScottPlot WpfPlot rendered in code-behind via item-loaded event -->
                  <Border x:Name="PlotBorder" Height="220" Tag="{Binding Request}" Loaded="OnPlotBorderLoaded"/>
                </StackPanel>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </ScrollViewer>
      </TabItem>

      <TabItem Header="Metrics">
        <DataGrid ItemsSource="{Binding Metrics}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Metric" Binding="{Binding Label}" Width="160"/>
            <DataGridTextColumn Header="Value"  Binding="{Binding Value}" Width="*"/>
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>

      <TabItem Header="Trades">
        <DataGrid ItemsSource="{Binding Trades}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  HeadersVisibility="Column">
          <DataGrid.Columns>
            <DataGridTextColumn Header="Time"   Binding="{Binding FilledAt, StringFormat='HH:mm:ss'}" Width="80"/>
            <DataGridTextColumn Header="Symbol" Binding="{Binding Symbol}"    Width="70"/>
            <DataGridTextColumn Header="Qty"    Binding="{Binding FilledQuantity}" Width="60"/>
            <DataGridTextColumn Header="Price"  Binding="{Binding FillPrice, StringFormat='N2'}" Width="80"/>
            <DataGridTextColumn Header="Comm."  Binding="{Binding Commission, StringFormat='N2'}" Width="70"/>
          </DataGrid.Columns>
        </DataGrid>
      </TabItem>

    </TabControl>
  </Grid>
</Page>
