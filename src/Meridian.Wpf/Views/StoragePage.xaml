<Page x:Class="Meridian.Wpf.Views.StoragePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{DynamicResource ShellWindowBackgroundBrush}"
      Loaded="OnPageLoaded"
      AutomationProperties.Name="Storage">

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20,28,32">
        <StackPanel MaxWidth="1200">
            <Border Style="{StaticResource EmbeddedShellHeroCardStyle}" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <Border Width="44" Height="44" CornerRadius="14"
                            Background="{StaticResource ConsoleAccentBlueAlpha20Brush}"
                            BorderBrush="{StaticResource InfoColorBrush}" BorderThickness="1">
                        <TextBlock Text="{StaticResource IconDatabase}"
                                   FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="24" Foreground="{StaticResource InfoColorBrush}"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Storage Management"
                                   Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="Capacity, layout conventions, and tiering controls for archive operations."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Storage Overview -->
            <Grid Margin="0,0,0,24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalSizeText}" FontSize="32" FontWeight="Bold"
                                   Foreground="{StaticResource InfoColorBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Total Size" HorizontalAlignment="Center"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding TotalFilesText}" FontSize="32" FontWeight="Bold"
                                   Foreground="{StaticResource SuccessColorBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Total Files" HorizontalAlignment="Center"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="4" Style="{StaticResource CardStyle}">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="{Binding SymbolCountText}" FontSize="32" FontWeight="Bold"
                                   Foreground="{StaticResource InfoColorBrush}"
                                   HorizontalAlignment="Center" />
                        <TextBlock Text="Symbols" HorizontalAlignment="Center"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Storage Configuration -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Storage Configuration" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" />

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Data Directory" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="DataDirectoryBox" Style="{StaticResource FormTextBoxStyle}"
                                     Text="./data" IsReadOnly="True" />
                            <Button Grid.Column="1" Content="Browse" Style="{StaticResource SecondaryButtonStyle}"
                                    Margin="8,0,0,0" />
                        </Grid>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Naming Convention" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <ComboBox x:Name="NamingConventionCombo"
                                  Style="{StaticResource FormComboBoxStyle}"
                                  SelectionChanged="StorageConfig_Changed">
                            <ComboBoxItem Content="By Symbol (symbol/type/date)" Tag="BySymbol" IsSelected="True" />
                            <ComboBoxItem Content="By Date (date/symbol/type)" Tag="ByDate" />
                            <ComboBoxItem Content="By Type (type/symbol/date)" Tag="ByType" />
                            <ComboBoxItem Content="Flat (symbol_type_date)" Tag="Flat" />
                        </ComboBox>
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Compression" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <ComboBox x:Name="CompressionCombo"
                                  Style="{StaticResource FormComboBoxStyle}"
                                  SelectionChanged="StorageConfig_Changed">
                            <ComboBoxItem Content="None" Tag="none" />
                            <ComboBoxItem Content="Gzip" Tag="gzip" IsSelected="True" />
                            <ComboBoxItem Content="LZ4 (Fast)" Tag="lz4" />
                            <ComboBoxItem Content="ZSTD (Best)" Tag="zstd" />
                        </ComboBox>
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Live File Tree Preview -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="{StaticResource IconFolder}"
                                   FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="20"
                                   Foreground="{StaticResource InfoColorBrush}"
                                   VerticalAlignment="Center" />
                        <TextBlock Text="File Structure Preview"
                                   Style="{StaticResource CardHeaderStyle}"
                                   Margin="8,0,0,0" />
                    </StackPanel>

                    <TextBlock Text="Preview of how data files will be organized on disk based on your current settings."
                               Foreground="{StaticResource ConsoleTextMutedBrush}"
                               TextWrapping="Wrap" Margin="0,0,0,12" />

                    <Border Background="{StaticResource ConsoleBackgroundMediumBrush}"
                            CornerRadius="12"
                            Padding="16">
                        <TextBlock Text="{Binding FileTreePreviewText}"
                                   FontFamily="Cascadia Code, Consolas, monospace"
                                   FontSize="12"
                                   Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                                   TextWrapping="NoWrap" />
                    </Border>

                    <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                        <TextBlock Text="{Binding StorageEstimateText}"
                                   Foreground="{StaticResource ConsoleTextMutedBrush}"
                                   FontSize="12" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Tiered Storage -->
            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Tiered Storage" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Margin="0,0,8,0">
                            <TextBlock Text="Hot Tier" FontWeight="SemiBold" Foreground="{StaticResource ConsoleTextPrimaryBrush}" />
                            <TextBlock Text="Last 7 days" Foreground="{StaticResource ConsoleTextMutedBrush}" FontSize="12" />
                            <TextBlock Text="{Binding HotTierSizeText}" Foreground="{StaticResource SuccessColorBrush}" FontWeight="SemiBold" Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="1" Margin="8,0">
                            <TextBlock Text="Warm Tier" FontWeight="SemiBold" Foreground="{StaticResource ConsoleTextPrimaryBrush}" />
                            <TextBlock Text="8-30 days" Foreground="{StaticResource ConsoleTextMutedBrush}" FontSize="12" />
                            <TextBlock Text="{Binding WarmTierSizeText}" Foreground="{StaticResource WarningColorBrush}" FontWeight="SemiBold" Margin="0,4,0,0" />
                        </StackPanel>

                        <StackPanel Grid.Column="2" Margin="8,0,0,0">
                            <TextBlock Text="Cold Tier" FontWeight="SemiBold" Foreground="{StaticResource ConsoleTextPrimaryBrush}" />
                            <TextBlock Text="30+ days" Foreground="{StaticResource ConsoleTextMutedBrush}" FontSize="12" />
                            <TextBlock Text="{Binding ColdTierSizeText}" Foreground="{StaticResource InfoColorBrush}" FontWeight="SemiBold" Margin="0,4,0,0" />
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
