<Page x:Class="Meridian.Wpf.Views.SetupWizardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:views="clr-namespace:Meridian.Wpf.Views"
      Background="{DynamicResource ShellWindowBackgroundBrush}"
      Loaded="OnPageLoaded"
      AutomationProperties.Name="Setup Wizard">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20,28,32">
        <StackPanel MaxWidth="1200">
            <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <Border Width="44" Height="44" CornerRadius="14"
                            Background="{StaticResource ConsoleAccentBlueAlpha20Brush}"
                            BorderBrush="{StaticResource InfoColorBrush}" BorderThickness="1">
                        <TextBlock Text="&#xE8D2;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="24" Foreground="{StaticResource InfoColorBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Setup Wizard" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="Get the backend connection, provider defaults, storage, and keys ready in one guided flow."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>
            <TextBlock Text="Let’s get your backend connection and configuration ready. Follow each step, then save &amp; continue."
                       Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,0,0,24" />

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Step 1 · Connect to the backend service" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />
                    <TextBlock Text="The app connects to http://localhost:8080. Check that the backend is running before continuing."
                               Foreground="{StaticResource ConsoleTextSecondaryBrush}" Margin="0,0,0,16" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <Ellipse x:Name="BackendStatusDot" Width="10" Height="10" Margin="0,0,8,0"
                                 VerticalAlignment="Center">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource ConsoleTextMutedBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding BackendStatusTone}" Value="Warning">
                                            <Setter Property="Fill" Value="{StaticResource WarningColorBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding BackendStatusTone}" Value="Success">
                                            <Setter Property="Fill" Value="{StaticResource SuccessColorBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding BackendStatusTone}" Value="Error">
                                            <Setter Property="Fill" Value="{StaticResource ErrorColorBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock x:Name="BackendStatusText" Text="{Binding BackendStatusText}"
                                   Foreground="{StaticResource ConsoleTextPrimaryBrush}" VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock x:Name="BackendStatusDetailText" Text="{Binding BackendStatusDetailText}"
                               Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,0,0,16" />

                    <WrapPanel>
                        <Button Content="Test Connection"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding TestConnectionCommand}" />
                        <Button Content="Start Backend"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding StartBackendCommand}" />
                        <Button Content="Open Instructions"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding OpenInstructionsCommand}" />
                    </WrapPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Step 2 · Choose your workflow (optional)" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />
                    <TextBlock Text="Select a preset that matches your use case. This pre-configures symbols, providers, and settings for your workflow."
                               Foreground="{StaticResource ConsoleTextSecondaryBrush}" Margin="0,0,0,16" />

                    <ItemsControl x:Name="PresetsList" ItemsSource="{Binding Presets}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <UniformGrid Columns="2" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button Margin="0,0,8,8"
                                        Command="{Binding DataContext.ApplyPresetCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                        CommandParameter="{Binding Id}"
                                        HorizontalContentAlignment="Stretch">
                                    <Button.Style>
                                        <Style TargetType="Button" BasedOn="{StaticResource SecondaryButtonStyle}">
                                            <Setter Property="Padding" Value="16,12" />
                                            <Setter Property="HorizontalAlignment" Value="Stretch" />
                                        </Style>
                                    </Button.Style>
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                                            <TextBlock Text="{Binding Icon}" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                                       FontSize="18" VerticalAlignment="Center" Margin="0,0,8,0" />
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center" />
                                        </StackPanel>
                                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap"
                                                   Foreground="{StaticResource ConsoleTextMutedBrush}" FontSize="12" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>

                    <TextBlock x:Name="PresetStatusText"
                               Text="{Binding PresetStatusText}"
                               Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,8,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Step 3 · Configure providers and storage" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />
                    <TextBlock Text="Choose your default data provider and where data will be stored."
                               Foreground="{StaticResource ConsoleTextSecondaryBrush}" Margin="0,0,0,16" />

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Configuration file" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <TextBlock x:Name="ConfigPathText" Text="{Binding ConfigPath}" Foreground="{StaticResource ConsoleTextMutedBrush}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Default provider" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <ComboBox x:Name="ProviderCombo"
                                  ItemsSource="{Binding ProviderOptions}"
                                  SelectedItem="{Binding SelectedProvider, Mode=TwoWay}"
                                  Style="{StaticResource FormComboBoxStyle}" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Storage location" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBox x:Name="StorageLocationTextBox"
                                     Style="{StaticResource FormTextBoxStyle}"
                                     Text="{Binding StorageLocation, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                     Grid.Column="0"
                                     Margin="0,0,12,0" />
                            <Button Content="Use Default"
                                    Style="{StaticResource SecondaryButtonStyle}"
                                    Grid.Column="1"
                                    Command="{Binding UseDefaultStorageCommand}" />
                        </Grid>
                    </StackPanel>
                    <TextBlock x:Name="ConfigStatusText"
                               Text="{Binding ConfigStatusText}"
                               Foreground="{StaticResource ConsoleTextMutedBrush}" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel>
                    <TextBlock Text="Step 4 · Add API keys (optional)" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />
                    <TextBlock Text="Provide API keys for premium providers. Keys are stored in your user environment variables."
                               Foreground="{StaticResource ConsoleTextSecondaryBrush}" Margin="0,0,0,16" />

                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Nasdaq Data Link API key" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <views:SecretInputControl x:Name="NasdaqApiKeyInput"
                                                  Secret="{Binding NasdaqApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  InputAutomationId="SetupWizardNasdaqApiKeyInput"
                                                  RevealAutomationId="SetupWizardNasdaqApiKeyRevealButton"
                                                  InputAutomationName="Nasdaq Data Link API key"
                                                  RevealAutomationName="Show or hide Nasdaq Data Link API key"
                                                  RevealToolTip="Show or hide Nasdaq Data Link API key" />
                    </StackPanel>

                    <StackPanel Margin="0,0,0,16">
                        <TextBlock Text="OpenFIGI API key (optional)" Style="{StaticResource FormLabelStyle}" Margin="0,0,0,4" />
                        <views:SecretInputControl x:Name="OpenFigiApiKeyInput"
                                                  Secret="{Binding OpenFigiApiKey, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                                  InputAutomationId="SetupWizardOpenFigiApiKeyInput"
                                                  RevealAutomationId="SetupWizardOpenFigiApiKeyRevealButton"
                                                  InputAutomationName="OpenFIGI API key"
                                                  RevealAutomationName="Show or hide OpenFIGI API key"
                                                  RevealToolTip="Show or hide OpenFIGI API key" />
                    </StackPanel>

                    <TextBlock x:Name="ValidationStatusText"
                               Text="{Binding ValidationStatusText}"
                               Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,0,0,16" />

                    <WrapPanel>
                        <Button Content="Save &amp; Continue"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Command="{Binding SaveAndContinueCommand}" />
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
