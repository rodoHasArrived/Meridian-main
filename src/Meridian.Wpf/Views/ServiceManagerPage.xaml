<Page x:Class="Meridian.Wpf.Views.ServiceManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource ShellWindowBackgroundBrush}"
      Loaded="OnPageLoaded"
      AutomationProperties.Name="Service Manager">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20,28,32">
        <StackPanel MaxWidth="1200">
            <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <Border Width="44" Height="44" CornerRadius="14"
                            Background="{StaticResource ConsoleAccentBlueAlpha20Brush}"
                            BorderBrush="{StaticResource InfoColorBrush}" BorderThickness="1">
                        <TextBlock Text="&#xE912;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="24" Foreground="{StaticResource InfoColorBrush}" VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Service Manager" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="Backend lifecycle controls, health posture, and runtime service registration for workstation operations."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Backend lifecycle" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding StatusText}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Health:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding HealthText}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Process ID:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding ProcessIdText}" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="Executable:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock Text="{Binding ExecutablePathText}" TextWrapping="Wrap" Width="900" />
                    </StackPanel>

                    <TextBlock Text="{Binding OperationResultText}" Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,0,0,16" />

                    <WrapPanel>
                        <Button Content="Install"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding InstallCommand}"
                                IsEnabled="{Binding CanInstall}" />
                        <Button Content="Start"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding StartCommand}"
                                IsEnabled="{Binding CanStart}" />
                        <Button Content="Stop"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding StopCommand}"
                                IsEnabled="{Binding CanStop}" />
                        <Button Content="Restart"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Command="{Binding RestartCommand}"
                                IsEnabled="{Binding CanRestart}" />
                        <Button Content="Refresh Status"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding RefreshCommand}"
                                IsEnabled="{Binding CanRefresh}" />
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
