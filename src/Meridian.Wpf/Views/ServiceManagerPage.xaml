<Page x:Class="Meridian.Wpf.Views.ServiceManagerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="{DynamicResource ConsoleBackgroundDarkBrush}"
      Loaded="OnPageLoaded"
      AutomationProperties.Name="Service Manager">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="24">
        <StackPanel MaxWidth="1200">
            <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                <TextBlock Text="&#xE912;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                           FontSize="28" Foreground="{StaticResource InfoColorBrush}" VerticalAlignment="Center" />
                <TextBlock Text="Service Manager" Style="{StaticResource PageTitleStyle}" Margin="12,0,0,0" VerticalAlignment="Center" />
            </StackPanel>

            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,24">
                <StackPanel>
                    <TextBlock Text="Backend lifecycle" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,16" />

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock x:Name="StatusText" Text="Unknown" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Health:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock x:Name="HealthText" Text="Unknown" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Process ID:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock x:Name="ProcessIdText" Text="-" />
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <TextBlock Text="Executable:" FontWeight="SemiBold" Margin="0,0,8,0" />
                        <TextBlock x:Name="ExecutablePathText" Text="Not registered" TextWrapping="Wrap" Width="900" />
                    </StackPanel>

                    <TextBlock x:Name="OperationResultText" Foreground="{StaticResource ConsoleTextMutedBrush}" Margin="0,0,0,16" />

                    <WrapPanel>
                        <Button x:Name="InstallButton"
                                Content="Install"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="Install_Click" />
                        <Button x:Name="StartButton"
                                Content="Start"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="Start_Click" />
                        <Button x:Name="StopButton"
                                Content="Stop"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="Stop_Click" />
                        <Button x:Name="RestartButton"
                                Content="Restart"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,12,0"
                                Click="Restart_Click" />
                        <Button x:Name="RefreshButton"
                                Content="Refresh Status"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Click="Refresh_Click" />
                    </WrapPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
