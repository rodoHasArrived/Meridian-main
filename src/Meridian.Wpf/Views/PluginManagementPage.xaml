<Page x:Class="Meridian.Wpf.Views.PluginManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:converters="clr-namespace:Meridian.Wpf.Converters"
      Background="{DynamicResource ShellWindowBackgroundBrush}"
      AutomationProperties.Name="Plugin Management">

    <Page.Resources>
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Page.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="28,20,28,32">
        <StackPanel MaxWidth="1200">

            <!-- Page header -->
            <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,24">
                <StackPanel Orientation="Horizontal">
                    <Border Width="44" Height="44" CornerRadius="14"
                            Background="{StaticResource ConsoleAccentBlueAlpha20Brush}"
                            BorderBrush="{StaticResource InfoColorBrush}" BorderThickness="1">
                        <TextBlock Text="&#xE74C;" FontFamily="{StaticResource SymbolThemeFontFamily}"
                                   FontSize="24" Foreground="{StaticResource InfoColorBrush}"
                                   VerticalAlignment="Center" HorizontalAlignment="Center" />
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Plugin Management" Style="{StaticResource PageTitleStyle}" />
                        <TextBlock Text="Load proprietary data source plugins from a directory. Each DLL is scanned for [DataSource]-decorated types."
                                   Style="{StaticResource CardDescriptionStyle}" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <!-- Directory selector card -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Plugin Directory" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <TextBox Grid.Column="0"
                                 Text="{Binding PluginsDirectory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 IsReadOnly="False"
                                 VerticalAlignment="Center"
                                 Padding="8,6"
                                 ToolTip="Path to the folder containing plugin DLL files" />

                        <Button Grid.Column="1"
                                Content="Browse…"
                                Command="{Binding BrowseCommand}"
                                Margin="8,0,0,0"
                                Padding="14,7"
                                VerticalAlignment="Center" />

                        <Button Grid.Column="2"
                                Content="Load Plugins"
                                Command="{Binding LoadPluginsCommand}"
                                Margin="8,0,0,0"
                                Padding="14,7"
                                VerticalAlignment="Center" />

                        <Button Grid.Column="3"
                                Content="Refresh"
                                Command="{Binding RefreshCommand}"
                                Margin="8,0,0,0"
                                Padding="14,7"
                                VerticalAlignment="Center" />
                    </Grid>

                    <!-- Loading indicator -->
                    <ProgressBar IsIndeterminate="True" Height="3" Margin="0,10,0,0"
                                 Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}" />
                </StackPanel>
            </Border>

            <!-- Plugins DataGrid card -->
            <Border Style="{StaticResource CardStyle}" Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="Loaded Plugins" Style="{StaticResource CardHeaderStyle}" Margin="0,0,0,12" />

                    <DataGrid ItemsSource="{Binding Plugins}"
                              IsReadOnly="True"
                              AutoGenerateColumns="False"
                              CanUserAddRows="False"
                              CanUserDeleteRows="False"
                              CanUserReorderColumns="False"
                              GridLinesVisibility="Horizontal"
                              HeadersVisibility="Column"
                              SelectionMode="Single"
                              MinHeight="160">
                        <DataGrid.Columns>

                            <DataGridTextColumn Header="Assembly"
                                                Binding="{Binding AssemblyFile}"
                                                Width="220" />

                            <DataGridTemplateColumn Header="Status" Width="100">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="4,0">
                                            <Ellipse Width="8" Height="8" Fill="{Binding StatusBrush}"
                                                     VerticalAlignment="Center" Margin="0,0,6,0" />
                                            <TextBlock Text="{Binding Status}"
                                                       Foreground="{DynamicResource ConsoleTextPrimaryBrush}" />
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>

                            <DataGridTextColumn Header="Registered Types"
                                                Binding="{Binding RegisteredTypes}"
                                                Width="*" />

                        </DataGrid.Columns>

                        <DataGrid.RowStyle>
                            <Style TargetType="DataGridRow" BasedOn="{StaticResource {x:Type DataGridRow}}">
                                <Setter Property="ToolTip" Value="{Binding AssemblyPath}" />
                            </Style>
                        </DataGrid.RowStyle>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Status bar -->
            <Border Style="{StaticResource CardStyle}">
                <TextBlock Text="{Binding StatusMessage}"
                           Foreground="{StaticResource ConsoleTextSecondaryBrush}"
                           FontSize="12"
                           TextWrapping="Wrap" />
            </Border>

        </StackPanel>
    </ScrollViewer>
</Page>
